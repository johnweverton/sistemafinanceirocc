# Story 12.4 — Fechar o loop do lançamento de faturamento em massa

## Status
Done — gate **PASS** (sem rework; ver
`docs/qa/gates/12.4-loop-lancamento-faturamento-massa.yml`)

## Story
**As a** operador que lança faturamento em massa antes do cálculo do lote contábil,
**I want** que o sistema só avance quando pelo menos um lançamento tiver sucesso, mostre as
falhas por nome, permita reenviar só os que falharam, e proteja contra trocar de competência com
valores digitados,
**so that** o passo 1 pare de mentir "pronto pra calcular" quando 100% dos lançamentos falharam
(bug de fluxo real, `RS-3`).

## Contexto
Fase 1 ALTA do épico `docs/architecture/epic-sanitizacao-emissoes.md`. Cobre R-2 (G-01, G-02,
G-03, G-04), `RS-3` (auditoria `docs/architecture/ux-gaps-emissoes.md`).

**[DEP]** 12.1 (usa `<Modal>`), 12.2 (`<CampoCompetencia>`), 12.3 (mesmo arquivo, contenção
`RS-9`).

## Acceptance Criteria
> Redação preservada do "critério de pronto" do épico.

1. O passo 1 só avança quando `lancados > 0`; com 12 de 12 falhas a tela **continua** no passo 1
   (hoje `setFaturamentoLancado(true)` roda incondicionalmente em
   `LoteContabilidadeDialog.tsx:53-60`).
2. As falhas são renderizadas **por nome do cliente** num bloco persistente (padrão já correto em
   `ClientesContabilidadeManager.tsx:278-286`), o que exige o backend
   (`cliente-contabilidade-faturamento-repository.ts:64-80`,
   `services/clientes-contabilidade.ts:66-69`) devolver `nome` em `falhas[]` — não só o UUID.
3. Botão "**Tentar de novo (N que falharam)**" remonta o passo 1 **só com os clientes pendentes**
   (hoje não existe: uma vez `faturamentoLancado=true`, `LoteContabilidadeDialog.tsx:121` nunca
   mais é verdadeiro).
4. Trocar a competência com valores digitados **limpa os valores ou pede confirmação explícita**
   ("manter os 12 valores digitados para 2026-08?") — hoje `:112-115` reseta `faturamentoLancado`
   mas mantém os valores, risco de lançar em massa na competência errada (`RS-3`).
5. Débito `DEB-12.3-B` (achado pelo `@qa` no gate da 12.3): a guarda de duplicidade (`comBoletoQ`,
   story 12.3) usa o `staleTime: 30_000` global de `app/providers.tsx:10` — o comentário local que
   diz "sem staleTime de propósito" é factualmente falso, `force-dynamic` só mata cache HTTP, não
   o cache do client; um diálogo aberto nunca revalida essa checagem. Corrigir para `staleTime: 0`
   nessa query específica e ajustar/remover o comentário incorreto. Além disso, o botão de lançar
   faturamento em massa (AC 1 desta story) deve respeitar `!guardaPronta` — hoje a guarda da 12.3
   (`D-1`, falha fechada) só bloqueia "Calcular", não "Lançar faturamentos", o que permite lançar
   faturamento em massa para um cliente que a guarda ainda não terminou de checar.

## Nota de sequenciamento (RS-9)
Segunda da cadeia `LoteContabilidadeDialog.tsx`: **12.3 → 12.4 → 12.5 → 12.6** (+ 12.8 na mesma
contenção). Não abrir antes de 12.3 fechar o gate de @qa.

## Complexidade e Riscos
- **Complexidade:** B/M (esforço do épico).
- **Risco `RS-3`** (lançamento na competência errada) — AC 4 é a mitigação direta; @qa deve
  testar o caminho "digitar valores → trocar competência → confirmar/cancelar" explicitamente.
- Depende do backend devolver `nome`, não só `clienteContabilidadeId` — mudança de contrato da API
  (`falhas[]`); checar consumidores existentes da resposta de `lancarFaturamentoLote`.
- **DoD:** AC 1–4 + testes verdes + File List completa.

## Tasks / Subtasks
- [x] Backend: `cliente-contabilidade-faturamento-repository.ts` + `services/clientes-contabilidade.ts`
      devolvem `nome` em `falhas[]` (AC 2)
- [x] `LoteContabilidadeDialog.tsx`: só avançar quando `lancados > 0` (AC 1)
- [x] `LoteContabilidadeDialog.tsx`: bloco persistente de falhas por nome (AC 2)
- [x] `LoteContabilidadeDialog.tsx`: botão "Tentar de novo (N)" remontando só pendentes (AC 3)
- [x] `LoteContabilidadeDialog.tsx`: guarda de troca de competência com valores digitados (AC 4)
- [x] `LoteContabilidadeDialog.tsx`: `staleTime: 0` na `comBoletoQ` + `!guardaPronta` no botão de
      lançar faturamentos (AC 5 — débito `DEB-12.3-B`)
- [x] Testes: 100% de falha não avança; parcial mostra falhas por nome; retry só reenvia
      pendentes; troca de competência limpa ou confirma

## Dev Notes
- Comportamento hoje documentado na auditoria (§2.7): "Todos os lançamentos falham →
  `faturamentoLancado = true` mesmo assim → avança para S1 → todos viram alerta" — classificado
  como **bug de fluxo**, não só gap de UX.
- Reuso: o padrão de renderizar falhas por chave+erro já existe e é correto em
  `ClientesContabilidadeManager.tsx:278-286` (importação de planilha) — replicar o mesmo padrão
  visual aqui, não inventar um novo.
- Este arquivo (`LoteContabilidadeDialog.tsx`) está em contenção com 12.3/12.5/12.6/12.8 (`RS-9`)
  — qualquer subtask aqui deve considerar o diff já aplicado por 12.3.

### File List (proposto)
- `apps/web/src/components/clientes-contabilidade/LoteContabilidadeDialog.tsx` (M — inclui AC 5:
  `staleTime: 0` da query `comBoletoQ` e gate `!guardaPronta` no botão de lançar faturamentos)
- `apps/web/src/services/clientes-contabilidade.ts` (M)
- `apps/web/src/app/api/clientes-contabilidade/faturamentos/lote/route.ts` (M — se necessário
  repassar `nome`)
- `apps/web/src/server/repositories/cliente-contabilidade-faturamento-repository.ts` (M)
- Testes: `LoteContabilidadeDialog.test.tsx` (M), `cliente-contabilidade-faturamento-repository.test.ts` (M)

## Testing
Vitest — repositório (`falhas[]` inclui `nome`), componente (0 sucesso não avança; N falhas
renderizadas por nome; "Tentar de novo" remonta só os pendentes; trocar competência com valores
digitados exige confirmação ou limpa).

## CodeRabbit Integration
- Story type: **Frontend + API (contrato de resposta)**.
- Foco: nenhuma regressão no caminho de 100% sucesso; contrato `falhas[]` com `nome` documentado;
  guarda de competência testada explicitamente (`RS-3`).
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## Dev Agent Record

### Agent Model Used
Claude Opus 5 (`claude-opus-5`) — @dev (Dex), modo autônomo (YOLO).

### Debug Log References
- `npx tsc --noEmit` — limpo.
- `npm run lint` (`next lint`) — "No ESLint warnings or errors".
- `npx vitest run` (suíte inteira, do zero) — **133 arquivos / 1201 testes verdes**.
  - 1ª execução teve 1 falha por *timeout* de 5s em `tests/server/gateway/boleto-emissao.test.ts`
    ("retorna 403 quando feature flag está desligada") — arquivo que esta story não toca. Rodado
    isolado: 11/11 verde; 2ª execução completa: 133/133 verde. Flake de contenção de CPU, não
    regressão.
- `npm run build` (`next build`) — sucesso.
- Teste do AC 5 validado como discriminante: removendo o `staleTime: 0` do componente, o caso
  "reabrir o diálogo revalida" FALHA (o `staleTime: 30_000` do teste, cópia do
  `app/providers.tsx`, serve o cache velho). Com o fix, passa.

### Completion Notes
**AC 1** — `setFaturamentoLancado(true)` saiu do topo do `onSuccess` e agora só roda com
`resultado.lancados > 0`. Com 0 lançados o passo 1 permanece montado (campos e botão na tela) e o
toast vira `error` em vez de `info`.

**AC 2** — Contrato de `falhas[]` mudou para `{ clienteContabilidadeId, nome, motivo }`, no mesmo
formato de `ExclusaoLoteResultado.bloqueados` (incluindo o `'—'` de fallback). O nome é resolvido
no repositório em **uma query só** (`listarClientesContabilidadePorIds`, já existente) e **só
quando há falha** — o caminho feliz não paga round-trip nenhum. A resolução de nomes é
best-effort: se ela falhar, o lote responde com `'—'` em vez de derrubar lançamentos que já deram
certo. Consumidores conferidos (`grep lancarFaturamentoLote|ResultadoLancamentoFaturamentoLote`):
só a rota (repassa `resultado` inteiro, sem mudança) e o diálogo — nenhum outro leitor de
`falhas[]` no repo, então a mudança de contrato é aditiva e não quebra ninguém.
Renderização reaproveita o padrão de `ClientesContabilidadeManager.tsx:265-285` (importação de
planilha): `div.alert-*` + `<p className="font-medium">` de resumo + `<ul className="mt-2 list-disc
space-y-1 pl-4 text-xs">` com `nome: motivo`. `alert-error` quando `lancados === 0`,
`alert-warning` no parcial (mesma regra do `erros.length === 0 ? success : warning` do Manager).

**AC 3** — Estado `pendentesRetry: string[] | null` filtra `alvosFaturamento` (novo derivado de
`faixaFaturamento`); o payload da mutation passou a ser construído a partir de `alvosFaturamento`,
então "Tentar de novo" **não reenvia** quem já foi lançado. Os valores digitados sobrevivem
(`faturamentos` é chaveado por id), então o operador revisa em vez de redigitar tudo.

**AC 4** — Escolhido o desenho **"limpa E pergunta"**: a troca de competência sempre zera os
valores digitados (estado seguro primeiro) e um `<ConfirmDialog tone="neutral">` (componente já
existente, reuso) oferece reaproveitá-los na competência nova, listando `nome: R$ valor` de cada um.
Cancelar/Escape/backdrop caem no lado seguro (campos em branco) — o caminho perigoso de `RS-3`
(manter 12 valores no mês errado) exige clique explícito em "Manter os N valores em AAAA-MM".
Trocar a competência também descarta `ultimoLancamento` e `pendentesRetry`, que pertencem ao mês
anterior.

**AC 5 (`DEB-12.3-B`)** — `staleTime: 0` **só na `comBoletoQ`** (o global de `app/providers.tsx`
continua 30s, intocado) e o comentário "Sem `staleTime` de propósito" foi reescrito explicando o
motivo real (`force-dynamic` mata cache HTTP, não o cache do react-query). O botão "Lançar
faturamentos e continuar" agora respeita `!guardaPronta` com o mesmo rótulo já usado em "Calcular"
("Verificando emissões…"), fechando o buraco de lançar faturamento em massa para um cliente que a
guarda ainda ia excluir.

**[AUTO-DECISION]** Botão de lançar desabilitado quando não há nenhum valor válido digitado
→ rótulo "Digite ao menos um faturamento" (razão: com lista vazia o POST só produzia um 422 do
`lancarFaturamentoLoteSchema` (`min(1)`), e sob a regra do AC 1 um 422 nunca faria o passo avançar
— o estado desabilitado explicado substitui um erro morto. Segue o padrão de rótulo-que-explica-o-
disabled já usado pelo botão "Calcular" na 12.3).

**[AUTO-DECISION]** Bloco de falhas visível apenas enquanto `!execucaoId` (razão: depois do
cálculo a tela é o resumo do lote, cujo painel é escopo declarado de 12.5/12.10; um "Tentar de novo"
ali seria inócuo, já que lançar faturamento depois do cálculo não muda o resultado já produzido).

**Fora de escopo confirmado:** nada de contador "X de Y preenchidos", ordenação por pendentes ou
`max-h` responsivo (**12.15**); nada do painel de composição/progresso (**12.5**); nenhum
`role="status"`/anúncio assistivo novo (**12.12**); `docs/design-system.md` e CSS global intocados.

### DoD (story-dod-checklist)
Todos os itens aplicáveis [x], com duas ressalvas honestas:
- **Verificação manual em navegador: [ ]** — a validação foi por teste de componente (Testing
  Library, 21 casos no arquivo do diálogo, incluindo os fluxos de tela dos 5 ACs) + `next build`.
  Não subi o app local nem exercitei o fluxo no browser; o @qa deve rodar o caminho
  "digitar valores → trocar competência → confirmar/cancelar" na mão, como a própria story pede.
- **Dependências novas: [N/A]** — nenhuma. Sem variável de ambiente, sem migration, sem mudança de
  schema Zod (o contrato que mudou é só o de *resposta*, `falhas[]`).

### File List
- `apps/web/src/components/clientes-contabilidade/LoteContabilidadeDialog.tsx` (M — AC 1–5)
- `apps/web/src/services/clientes-contabilidade.ts` (M — `nome` em `falhas[]`)
- `apps/web/src/server/repositories/cliente-contabilidade-faturamento-repository.ts` (M — `nome`
  em `falhas[]` + `resolverNomesDasFalhas`)
- `apps/web/tests/components/LoteContabilidadeDialog.test.tsx` (M — +11 testes: AC 1–5)
- `apps/web/tests/server/repositories/cliente-contabilidade-faturamento-repository.test.ts` (M —
  +4 testes de `lancarFaturamentoLote`, incluindo `nome` nas falhas e falha na resolução de nomes)
- `apps/web/src/app/api/clientes-contabilidade/faturamentos/lote/route.ts` — **verificado, sem
  alteração**: a rota já devolve o `resultado` inteiro do repositório, então `nome` passa por ela
  sem mudança de código (estava no File List proposto com a ressalva "se necessário").

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-08-25 | 0.1 | Story criada pelo @sm a partir do Épico 12 (`docs/architecture/epic-sanitizacao-emissoes.md`, Fase 1, story 12.4). Aguarda `*validate-story-draft` do @po antes de @dev iniciar. | @sm |
| 2026-08-25 | 0.2 | @po `*validate-story-draft` (po-master-checklist): GO. Os 4 ACs mapeiam 1:1 em G-01..G-04 e são testáveis; mudança de contrato (`nome` em falhas[]) explicitada com aviso de checar consumidores; [DEP] 12.1/12.2/12.3 correta e sem ciclo; posição 2 da cadeia RS-9 declarada. Aprovada sem ajuste. Status Draft -> Ready. | @po |
| 2026-08-26 | 1.0 | @dev implementou os 5 ACs. Backend devolve `nome` em `falhas[]` (1 query, só quando há falha, best-effort); passo 1 só avança com `lancados > 0`; bloco persistente de falhas por nome no padrão da importação de planilha; "Tentar de novo (N que falharam)" remonta o passo só com pendentes; troca de competência limpa os valores e pede confirmação explícita pra reaproveitá-los (`ConfirmDialog` reusado); `DEB-12.3-B` fechado (`staleTime: 0` local + `!guardaPronta` no botão de lançar). 15 testes novos/atualizados, suíte 1201/1201, lint/tsc/build verdes. Status Ready -> Review. | @dev |

## QA Results

**Verdict: PASS** (iteração 1) — gate em
`docs/qa/gates/12.4-loop-lancamento-faturamento-massa.yml`. Commit revisado: `382a366`.

**PASS sem rework, e a fila `RS-9` avança: 12.5 pode abrir.** Os 5 ACs estão implementados como
escritos, `DEB-12.3-B` está fechado exatamente na forma prescrita pelo gate da 12.3, e o bug de
fluxo do `RS-3` está morto e travado por teste. O que sustenta o veredito não é a leitura do diff —
é a prova de discriminância abaixo.

- **Testes discriminantes provados por mutação, 4/4 exatos.** Não aceitei a afirmação do @dev nem
  minha própria leitura: mutei o `src` em 4 pontos independentes e rodei. `if (resultado.lancados
  === 0)` → `if (false)` (reproduz o comportamento pré-12.4) ⇒ **1 falha**, "AC 1: 100% de falha NÃO
  avança"; remover `setFaturamentos({})` ⇒ **1 falha**, "AC 4: … limpa os campos e pede
  confirmação"; remover `staleTime: 0` ⇒ **1 falha**, "AC 5 (DEB-12.3-B) … reabrir o diálogo
  revalida" (`expected "spy" to be called 2 times, but got 1 times`); remover `|| !guardaPronta` do
  `disabled` **deixando o rótulo intacto** ⇒ **2 falhas**, os dois casos de AC 5. Em nenhuma houve
  falha colateral nem falha a menos: o mapeamento AC↔teste é exato. Árvore restaurada com
  `git checkout --` após cada mutação; `git status -- apps/web` vazio ao final.
- **Números reexecutados por mim.** `tsc --noEmit` exit 0; `next lint` "No ESLint warnings or
  errors"; `vitest run` **duas vezes**, 133 arquivos / **1201 testes verdes** nas duas (bate com o
  Dev Agent Record e com o delta do gate da 12.3: 1186 → 1201 = +15). `next build` verde — a 1ª
  tentativa saiu com `EINVAL readlink` dentro de `next/dist/lib/recursive-delete.js`, que é artefato
  stale de `.next` em pasta OneDrive e **não é código**; limpei e rodou exit 0, 59/59 páginas.
  Métrica verificável no lugar de "N rotas": **77 → 77** arquivos de rota (nenhuma adicionada nem
  removida), coerente com "rota verificada, sem alteração".
- **AC 4 — desenho "limpa E pergunta" APROVADO.** O "ou" da AC não era exclusivo: o próprio parêntese
  dela ("manter os 12 valores digitados para 2026-08?") só faz sentido se o padrão for limpar. Cada
  alternativa isolada é pior — só limpar pune quem troca de mês (e o operador punido deixa de
  trocar, virando incentivo a `RS-3`); só perguntar faz o caminho passivo (Escape/backdrop) cair no
  lado perigoso. O entregue converge `onCancel`/Escape/backdrop no lado seguro e exige clique
  afirmativo num botão que **nomeia o mês de destino**, com a lista `nome: R$ valor` à vista. É o
  mesmo princípio de modo de falha que aprovei na 12.3 (D-1, guarda que falha fechada). E o teste
  mira a camada certa: `toHaveBeenCalledWith('2026-07', …)` — a competência do **payload**, que é
  onde `RS-3` se materializa. Ressalva registrada: não confundir com o rascunho em `localStorage`
  que a 12.15 veta — aqui é em sessão, por ocorrência e itemizado.
- **AC 5 confirmado nas três partes.** `staleTime: 0` só na `comBoletoQ` (`:64-69`); o global de
  `app/providers.tsx` **não** está no diff e segue 30s; o comentário mentiroso da 12.3 foi
  reescrito explicando a diferença entre cache HTTP e cache do react-query; e `:362` tem
  `|| !guardaPronta` com cobertura nos **dois** modos (guarda pendente e guarda em erro) — o que
  resolve a contradição D-1/D-2 que registrei em OBS-123-4b. `DEB-12.3-B` pode ser marcado como
  resolvido.
- **Resolução de nomes best-effort: segura por construção, não por convenção.** Os `upsert` já estão
  commitados quando `resolverNomesDasFalhas` roda; a função é `void`, muta in-place e o único erro
  possível (`ApiError 500` de `listarClientesContabilidadePorIds`) morre no `catch`. Não existe
  caminho que altere `resultado.lancados`. E está travado por teste ("busca de nomes falhando não
  derruba o lote", `expect(r.lancados).toBe(1)`), mais os contadores `buscasDeClientes` `toBe(0)` no
  caminho feliz e `toBe(1)` na falha parcial — que travam de uma vez o "sem round-trip no caminho
  feliz" e a ausência de N+1. **Lançamento confirmado não pode ser perdido para buscar um nome.**
- **Escopo e regressões.** 6 arquivos no commit; **nenhum `.css`, nenhum `docs/design-system.md`**,
  nenhuma migration, nenhum token. Zero `brl`/`normalizarBusca`/`competenciaAtual` local
  reintroduzido e zero `<input type="text">` de competência (regressão da 12.2: limpa). Os testes de
  **ausência** do Cenário A da 12.3 (`queryByRole('checkbox')`, `queryByText(/emitir mesmo assim/i)`)
  seguem intactos em `:123-124` — nenhum opt-in voltou. Contenção `RS-9` **íntegra**: o bloco "Pronto
  pra calcular" está palavra por palavra como estava (é o que a AC 1 da 12.5 vai substituir), sem
  contador da 12.15, sem `max-h` mexido, sem `role="status"` da 12.12.
- **Flake do @dev: não reproduzi, e a atribuição procede.** Rodei a suíte inteira 2× sem nenhuma
  falha. O `boleto-emissao.test.ts` não tem interseção de código com este diff (último toque em
  `0d4bf47`), e o caso que estourou é o **primeiro `it` do arquivo**, cuja 1ª instrução é um
  `await import('@/app/api/boletos/emitir/route')` — cold start de transformação do grafo do Next
  cobrado dentro do timeout de 5s do próprio teste. Timeout, não asserção; 1201 em todas as
  execuções. Não-relacionado, sem ação.

### Achados (nenhum bloqueante)

- `OBS-124-1` (low) — o comentário `:305-308` diz que o bloco de falhas "sobrevive ao avanço pro
  passo de cálculo… vai virar alerta lá", mas o `!execucaoId` da linha seguinte o remove exatamente
  aí. O **comportamento** está certo (aprovado como D-4: a informação reaparece como `alerta` do
  resultado em `:405-415`); o registro é que está errado. Levanto porque é **a mesma classe de
  defeito que motivou o AC 5 desta story** — corrigimos um comentário mentiroso e introduzimos outro.
- `OBS-124-2` (low) — "Cancelar/Escape/backdrop caem no lado seguro" é **verdade** (verifiquei a
  composição `ConfirmDialog` → `Modal.tentarFechar` → `onCancel`), mas só o Cancelar tem asserção.
  Numa story cujo desenho inteiro se apoia em "toda saída ambígua é segura", 2/3 sem trava é a
  lacuna que eu apontaria mesmo num commit impecável. Dois `it` resolvem.
- `OBS-124-3` (low) — o `<input type="month">` pode ser **limpo**; com `competencia === ''` a query
  fica `enabled: false`, então nem `success` nem `error`: os dois botões ficam desabilitados sob
  **"Verificando emissões…"**, rótulo falso, e o bloco de erro não aparece. É melhora sobre o
  anterior (antes o clique dava 422) e é a mesma raiz do OBS-123-4a que já roteei para a 12.6 — o
  ternário precisa de 3 ramos, não 2.
- `OBS-124-4` / `OBS-124-5` / `OBS-124-6` / `OBS-124-7` (info) — pior caso do `.in()` com 200 ids
  (degrada seguro por desenho); valores já lançados sobrevivem em `faturamentos` e entram no
  `ConfirmDialog` descritos como "digitados" (não é defeito — dois atos afirmativos e `unique
  (cliente, competência)` da 0031 impede duplicação; **não** "consertar" limpando no `onSuccess`,
  isso quebraria o AC 3); `DEB-12.3-D` cresceu (arquivo 326 → **457** linhas, deslocamento acumulado
  > 200); e registro positivo pelo `[ ]` honesto do @dev na verificação manual.

### Limitação desta verificação — verificação manual em navegador NÃO foi feita

A story pede que o @qa rode "digitar valores → trocar competência → confirmar/cancelar" à mão.
**Não subi o app no navegador** — este ambiente não tem navegador disponível. Não vou carimbar como
feito o que não foi feito. No lugar: leitura do caminho no código, testes de componente, `next build`
e as 4 mutações acima. A cobertura de componente compensa as 4 ramificações do AC 4 (limpar +
confirmação, Cancelar, Confirmar com asserção de payload na competência nova, e troca direta sem
valores). **Não** compensa: empilhamento visual do modal-dentro-de-modal (jsdom não avalia `z-index`;
risco baixo por precedente — `SyncModal` → `ConfirmDialog` já roda em produção), foco real
ao abrir/fechar a confirmação, e o seletor nativo de `<input type="month">`. Recomendo antes do
deploy uma passada de ~3 min só nisso: confirmação por cima do diálogo com o campo de trás inerte,
Escape/backdrop deixando os campos vazios, e limpar o mês não virando beco. **Não bloqueia o gate.**

### Débitos registrados

- `DEB-12.4-A` (low) → **12.5**: comentário de `:305-308` + asserções de Escape/backdrop.
- `DEB-12.4-B` (low) → **12.6**: 3º ramo de rótulo (competência não informada ≠ pendente ≠ erro),
  agrupado com `OBS-123-4a`.
- `DEB-12.4-C` (info) → **12.12**: o bloco de falhas por nome virou o candidato nº1 a `role="alert"`
  e **não está em nenhuma AC da 12.12** (o padrão copiado, `ClientesContabilidadeManager.tsx:263`,
  usa `role="alert"`; a cópia trouxe o visual e deixou a semântica).
- Reafirmados **sem dono**: `DEB-12.3-A` (a 12.4 fechou a **camada 2** de `RS-1` — cache de cliente;
  camadas 1 e 3 seguem abertas, então **`RS-1` continua REDUZIDO, NÃO FECHADO**), `DEB-12.3-C` (o
  fluxo médico ficou ainda mais atrás, e o comentário mentiroso sobre cache agora existe em
  exatamente um lugar: `NovaExecucao.tsx`) e `DEB-12.2-A`.

**Atenção do @po — padrão recorrente deste épico (4ª vez):** `DEB-12.4-A`, `DEB-12.4-B` e
`DEB-12.4-C` vão para stories com AC **enumerada e fechada**, que não absorve item novo por herança.
"A story já abre esse arquivo" não basta — cada um precisa de acréscimo explícito à AC **e** à File
List da story de destino, senão morre no Dev Agent Record. O acréscimo do `comBoletoQ` à AC 1 da
12.6, que pedi no gate da 12.3, **continua pendente**.