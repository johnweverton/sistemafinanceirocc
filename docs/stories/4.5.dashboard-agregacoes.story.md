# Story 4.5 — Agregações do Dashboard financeiro (RPC/views)

## Status
Ready for Review

## Story
**As a** gestor do escritório,
**I want** funções/visões de agregação financeira (por competência, por médico e aging de vencidos),
**so that** o dashboard (4.6) leia KPIs prontos sem cálculo pesado no cliente.

## Acceptance Criteria
1. Agregação **por competência**: total emitido, recebido, em aberto, vencido e **taxa de
   inadimplência** (RPC `rpc_dashboard_por_competencia` ou view).
2. Agregação **por médico**: mesmos totais + ticket médio.
3. **Aging** de vencidos em faixas: 0–30, 31–60, 60+ dias (com base em `vencimento`).
4. Repositório tipado em `shared` (`ResumoCompetencia`, `ResumoMedico`, `AgingFaixa`) + funções de
   leitura no backend.
5. `typecheck`, `lint`, `test` verdes.

## Complexidade e Riscos
- **Complexidade:** M (~3 pontos). Concentra-se em SQL (RPC/views) + tipos + leitura.
- **Escopo OUT:** a UI do dashboard (4.6).
- **Riscos:** correção das agregações (reusar a MESMA regra de status derivado da 4.4 para não
  divergir). *Mitigação:* centralizar a regra numa view base reutilizada pelas agregações.
- **DoD:** AC 1-5 + testes verdes + File List completa.

## Tasks / Subtasks
- [x] RPC/views de agregação (AC: 1-3) — **@data-engineer** (migration 0009)
  - [x] Views `vw_dashboard_competencia`, `vw_dashboard_medico`, `vw_dashboard_aging` reusando
        `vw_recebiveis` como base (status derivado único); todas `security_invoker=on`
- [x] Tipos + repositório (AC: 4)
  - [x] `ResumoCompetencia`/`ResumoMedico`/`AgingFaixa` em `shared` + mappers snake→camel
  - [x] `dashboard-repository.ts`: `resumoPorCompetencia(competencia?)`, `resumoPorMedico`, `aging`
- [x] Rotas `GET /api/dashboard/{competencias,medicos,aging}` (requireRole admin/financeiro) (AC: 4)
- [x] Testes (AC: 5)
  - [x] repositório: mapeamento das 3 agregações + filtro `.eq` (mock)
- [x] `typecheck`, `lint`, `test` (132 verdes)

## Dev Notes
**Depende de 4.4** (modelo de recebíveis / status derivado).

- **Consistência é crítica:** a taxa de inadimplência e os totais devem usar a mesma definição de
  status derivado da 4.4. Preferir uma **view base** (ex.: `vw_recebiveis`) e agregar em cima dela,
  para não duplicar a regra em dois lugares.
- Aging só considera boletos vencidos e sem baixa; faixas por `current_date - vencimento`.
- Delegar o SQL ao @data-engineer; o dev integra tipos + leitura + rota.

### Arquivos afetados
- `supabase/migrations/` (RPC/views — @data-engineer)
- `packages/shared/src/types/*` (tipos de agregação)
- `apps/web/src/server/repositories/*` (leitura das agregações)
- `apps/web/src/app/api/dashboard/*` (rotas)
- Testes

## Testing
- Vitest. Leitura com Supabase admin mockado (linhas de agregação fixas).

## CodeRabbit Integration
- Story type: **Feature (dados/agregação)**.
- Foco: correção e consistência das agregações, índices adequados, autorização.
- Severidade: CRITICAL/HIGH auto-fix; performance de agregação como HIGH se houver full scan.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-02 | 0.1 | Story criada a partir da arquitetura §6 | River (@sm) |
| 2026-07-02 | 1.0 | Validada 10/10. GO (RPC/views = migração c/ @data-engineer). Status Draft→Ready | Pax (@po) |
| 2026-07-03 | 1.1 | Implementada (views 0009 + tipos + repositório + rotas). Status Ready→Ready for Review | Dex (@dev) |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (Dex / @dev), modo *develop

### Completion Notes List
- **Views (0009, @data-engineer):** `vw_dashboard_competencia/medico/aging` reusam `vw_recebiveis`
  (regra de status única) com `FILTER (WHERE ...)`, divisão segura (`nullif`+`coalesce`), `security_invoker=on`.
- **Tipos/mappers:** `ResumoCompetencia`/`ResumoMedico`/`AgingFaixa` + Row types + `num()` coalesce.
- **Repositório:** `resumoPorCompetencia(competencia?)` filtra `.eq('competencia', x)`; `resumoPorMedico`
  ordena por total_emitido; `aging` global.
- **Rotas:** 3 GETs sob `/api/dashboard/*` (requireRole admin/financeiro).
- **DECISÃO documentada:** `vw_dashboard_medico`/`vw_dashboard_aging` agregam **sem** competência, então
  nesta 1ª versão o filtro de competência aplica só ao resumo por competência (KPIs); médico e aging
  são **globais**. Adicionar `competencia` a essas views (para filtro) é follow-up.
- Testes: 126 → **132** (repositório +4; componente da 4.6 +2). CodeRabbit: pulado (WSL não auth).

### File List
- `packages/shared/src/types/dashboard.ts` (A), `.../index.ts` (M)
- `apps/web/src/server/repositories/mappers.ts` (M) — Resumo*/Aging Row+mappers
- `apps/web/src/server/repositories/dashboard-repository.ts` (A)
- `apps/web/src/app/api/dashboard/{competencias,medicos,aging}/route.ts` (A x3)
- `apps/web/tests/server/repositories/dashboard-repository.test.ts` (A)
- (view SQL: `supabase/migrations/0009_dashboard_agregacoes.sql` — @data-engineer)

## QA Results

### Gate: PASS ✅ — Quinn (@qa), 2026-07-03

**Escopo:** camada de dados das agregações do Dashboard (views 0009 + tipos + repositório + rotas).
Verificação independente: `typecheck` limpo; testes do dashboard-repository 4/4 verdes; suíte total 132.
CodeRabbit: não executado (WSL não autenticado) — sem impedimento para esta camada.

**Traceability (AC → evidência):**
- AC1 (competência: emitido/recebido/em aberto/vencido + inadimplência) → `vw_dashboard_competencia`
  com `FILTER (WHERE status_derivado=...)` + `resumoPorCompetencia` + teste snake→camel. ✅
- AC2 (médico + ticket médio) → `vw_dashboard_medico` (`ticket_medio = emitido/nullif(count,0)`) +
  `resumoPorMedico` (order by total_emitido) + teste ticketMedio. ✅
- AC3 (aging 0-30/31-60/60+ por `vencimento`) → `vw_dashboard_aging` (filtra `status_derivado='vencido'`
  e `vencimento is not null`) + teste faixas. ✅
- AC4 (tipos shared + leitura) → `ResumoCompetencia/ResumoMedico/AgingFaixa` + mappers com `num()`
  coalesce + `dashboard-repository`. ✅
- AC5 (gates) → typecheck/lint/132. ✅

**NFR:**
- **Segurança: PASS.** As 3 rotas chamam `requireRole(['admin','financeiro'])`; guarda barra 401 (sem
  sessão), 403 (sem perfil — sem escalonamento de privilégio) e 403 (papel não autorizado). Views
  `security_invoker=on` → aplicam a RLS de `boletos` a quem consulta (defesa em profundidade), ainda que
  o app leia via service role. Divisão segura (`nullif`+`coalesce`) evita divisão por zero. Nenhum
  segredo/PII além do esperado para admin/financeiro (nome do médico).
- **Consistência (crítica desta story): PASS.** As três agregações reusam `vw_recebiveis` como base →
  a definição de status derivado é ÚNICA; taxa de inadimplência e totais não divergem de Contas a
  Receber. Atende diretamente ao risco/mitigação declarado na story.
- **Performance: PASS.** Volume ~120 boletos/mês; agregação com FILTER sobre a view é barata; índices de
  0004/0007 cobrem os joins. Sem novos índices — justificado.
- **Confiabilidade: PASS.** Erros de DB encapsulados em `ApiError(500, 'DB_ERROR')`.

**Observação anterior (Low) — RESOLVIDA (migration 0010):**
~~`vw_dashboard_medico`/`vw_dashboard_aging` não expunham `competencia`~~ → corrigido na migration
0010 via GROUPING SETS: todas as views agora têm a coluna `competencia` e geram linhas por competência
+ linha de rollup (IS NULL = total geral). Repositório/rotas aceitam `?competencia`. Testes ampliados
(`.is(null)` / `.eq(competencia)`).

**Veredito:** PASS. Camada de dados correta, segura e consistente. Liberado para commit/push.

— Quinn, guardião da qualidade 🛡️

