# Story 7.3 — UI da conta emissora: cadastro, confirmação de emissão e recebíveis

## Status
Done

## Story
**As a** coordenadora/equipe financeiro,
**I want** definir a empresa emissora de cada médico no cadastro, ver por qual empresa o boleto
sairá antes de confirmar a emissão e enxergar/filtrar a empresa nos recebíveis,
**so that** nenhum boleto saia pela empresa errada e a carteira fique legível por empresa.

## Contexto
Fecha o Épico 7 na camada de experiência. Backend pronto nas 7.1/7.2. Superfícies: MedicoForm
(cadastro), diálogo de confirmação de emissão (RelatorioGrupos) e Recebíveis. A UX do cliente
final (beneficiário e remetente corretos) já é garantida pela 7.2 — aqui é a barreira humana
interna contra erro de classificação.

## Acceptance Criteria
1. **Cadastro do médico (MedicoForm):** select **"Empresa emissora"** (rótulos `nomeExibicao`:
   "MC" / "Cavalcante Viana"), obrigatório no submit; médicos existentes exibem o valor do
   backfill (`mc`). Alteração registrada em `medicos_historico` (mecanismo existente) e listada
   no detalhe do médico.
2. **Confirmação de emissão:** o diálogo de emitir boleto exibe a empresa emissora do médico
   (ex.: "Boleto será emitido por **Cavalcante Viana**") — informação visível ANTES do clique
   de confirmação.
3. **Recebíveis:** coluna/badge "Empresa" (dados de `vw_recebiveis.conta_emissora`, já exposto
   pela 7.1) + filtro por empresa no padrão dos filtros existentes; badge segue a identidade
   visual dos badges atuais (disparo WhatsApp/e-mail).
4. **CSV de importação (se aplicável):** coluna opcional `conta_emissora` no modelo estendido
   da story 3.4 (valores `mc`/`cavalcante_viana`; ausente → default `mc`). Validação com
   mensagem clara em valor inválido.
5. Acessibilidade e tema: select/badge funcionam em light/dark (tokens CSS existentes).
6. `npm run typecheck`, `lint` e `test` verdes; testes de componente para o select obrigatório
   e para o filtro de recebíveis.

## Complexidade e Riscos
- **Complexidade:** S/M (~2-3 pontos). Padrões de UI já existem (selects do MedicoForm,
  badges/filtros de Recebíveis).
- **Escopo OUT:** segmentação do dashboard por empresa (fase 2, arquitetura §6); override de
  conta por emissão (D1-C, sem caso de uso).
- **Riscos:** classificação errada em massa na virada — *mitigação:* AC 4 (CSV) + a lista de
  médicos da CV é conhecida da coordenação; conferência via filtro de Recebíveis após as
  primeiras emissões.
- **DoD:** AC 1–6 + File List completa.

## Tasks / Subtasks
- [x] MedicoForm: select obrigatório + Zod + histórico (AC: 1) — Zod da 7.1; histórico automático (diff genérico por campo)
- [x] Diálogo de emissão: exibir empresa emissora (AC: 2) — `EmitirBoletoDialog` novo (emissão deixou de ser clique direto)
- [x] Recebíveis: badge/coluna "Empresa" + filtro (AC: 3) — view→repo→rota→service→UI
- [x] CSV: coluna opcional `conta_emissora` + validação via schema (AC: 4)
- [x] Tema/acessibilidade (AC: 5) — tokens/classes existentes (input, badge-slate, role=alert, aria-invalid)
- [x] Testes de componente + `typecheck`/`lint`/`test` (AC: 6) — 294 testes verdes (274 web + 20 shared)
- [x] EXTRA (débito D-721, gate 7.2): emissão com conta sem credenciais → `ApiError 503 CONTA_NAO_CONFIGURADA` + toast claro na UI

## Dev Notes
- MedicoForm e ViaCEP: story 3.3 (padrão de campos/Zod). Histórico de alteração já é genérico
  por campo (`medicos_historico`, 0001).
- Emissão na UI: `apps/web/src/services/boletos.ts` + componente `RelatorioGrupos` (a rota
  devolve a conta desde a 7.2; para o AVISO pré-confirmação, usar a conta do médico carregado).
- Recebíveis: `RecebiveisManager.tsx`; padrão de badge da feature de disparos (commit a110bb5).
- Rótulos SEMPRE via `CONTAS_EMISSORAS[conta].nomeExibicao` — nunca string solta na UI.

### Arquivos afetados (estimativa)
- `apps/web/src/components/**/MedicoForm*.tsx` (M)
- Componente do diálogo de emissão (RelatorioGrupos) (M)
- `apps/web/src/components/**/RecebiveisManager.tsx` (M)
- Parser/modelo CSV (M — story 3.4 como base)
- Testes de componentes

## Testing
Vitest + Testing Library. Casos-chave: submit sem empresa → erro de validação; diálogo mostra
o nome da empresa do médico; filtro de recebíveis reduz a lista por empresa; CSV com valor
inválido → mensagem clara.

## File List
- `packages/shared/src/types/conta-emissora.ts` (M) — `CONTA_EMISSORA_LABEL` (fonte única de rótulos)
- `packages/shared/src/types/recebivel.ts` (M) — `FiltroRecebiveis.contaEmissora`
- `apps/web/src/server/gateway/contas-emissoras.ts` (M) — `nomeExibicao` deriva do shared
- `apps/web/src/server/repositories/recebiveis-repository.ts` (M) — filtro por conta
- `apps/web/src/app/api/recebiveis/route.ts` (M) — query param `conta` (Zod)
- `apps/web/src/app/api/boletos/emitir/route.ts` (M) — D-721: 503 `CONTA_NAO_CONFIGURADA`
- `apps/web/src/server/csv/medicos-import.ts` (M) — coluna opcional `conta_emissora`
- `apps/web/src/services/medicos.ts` (M) — `NovoMedicoPayload.contaEmissora`
- `apps/web/src/services/recebiveis.ts` (M) — qs `conta`
- `apps/web/src/components/medicos/MedicoForm.tsx` (M) — select "Empresa emissora" obrigatório
- `apps/web/src/components/execucoes/RelatorioGrupos.tsx` (M) — `EmitirBoletoDialog` + toast D-721
- `apps/web/src/components/recebiveis/RecebiveisManager.tsx` (M) — coluna/badge + filtro
- Testes (M/A): `MedicoForm.test.tsx`, `RelatorioGrupos.test.tsx`, `RecebiveisManager.test.tsx`, `importar-csv.test.ts`, `boleto-emissao.test.ts` (503, QA-731-2)
- `apps/web/public/templates/medicos-modelo.csv` (M) — coluna `conta_emissora` + exemplos (QA-731-1)
- `apps/web/public/templates/medicos-modelo.xlsx` (M) — regenerado a partir do CSV, 20 colunas (QA-731-1)

## Dev Agent Record
### Agent Model Used
claude-fable-5 (Dex / @dev), via /full-sdc interactive

### Decisões de implementação
- **Emissão ganhou diálogo de confirmação** (antes era clique direto): a empresa emissora
  precisa ser visível ANTES do clique final (AC 2). A conta do médico vem de
  `medicosService.listar()` cacheado pelo react-query; confirmar fica desabilitado até a
  empresa resolver (nunca emite "às cegas").
- Rótulos centralizados em `CONTA_EMISSORA_LABEL` no shared (client-safe); o registro
  server (`contas-emissoras.ts`) passou a derivar de lá — fonte única, sem string solta.
- `MedicoForm`: estado interno admite `''` (novo médico começa sem escolha — decisão
  consciente do operador); `onSubmit` expõe payload fechado com a conta garantida.
- Histórico de alteração de `contaEmissora` sai de graça: o diff de `atualizarMedico` é
  genérico por campo top-level.
- CSV: campo só entra no input quando a coluna vem preenchida (mesmo padrão condicional
  do `criarBoleto`); valor inválido reprova a LINHA pelo schema, sem abortar o lote.

## CodeRabbit Integration
- Story type: **Feature (UI)**.
- Foco: validação de formulário, consistência de rótulos via registro, acessibilidade.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## QA Results
**Iteração 2 (2026-07-10): PASS** — QA-731-1/2 verificados corrigidos; OBS-732 deferido.
Débito D-721 (gate 7.2) resolvido nesta entrega. Gate final em
`docs/qa/gates/7.3-ui-conta-emissora.yml`.

**Iteração 1 (2026-07-10): CONCERNS com fixes exigidos** — gate
`docs/qa/gates/7.3-ui-conta-emissora.yml`.
- **QA-731-1 (MEDIUM):** modelos baixáveis (csv/xlsx) sem a coluna `conta_emissora`.
- **QA-731-2 (MEDIUM):** remediação D-721 sem teste de rota (503).
- **OBS-732 (LOW, aceito):** a11y de diálogos é padrão global do projeto, fora do escopo.

**Resolução @dev (2026-07-10):**
- QA-731-1 **FIXED** — coluna `conta_emissora` nos dois modelos (csv com exemplos
  mc/cavalcante_viana/vazio; xlsx regenerado do csv e validado por round-trip de leitura).
- QA-731-2 **FIXED** — teste de rota: factory lança → 503 `CONTA_NAO_CONFIGURADA`, mensagem
  nomeia a conta, nada emitido/persistido. De quebra documentado o rate limiter como estado
  de módulo nos testes (userId único por teste novo).
- OBS-732 **DEFERRED** (dono) — story transversal de acessibilidade.
- Gates re-executados: typecheck/lint OK, **295 testes verdes** (275 web + 20 shared).

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-10 | 0.1 | Story criada a partir da arquitetura multi-conta (D1/D2/D3 aprovadas pelo dono) | River (@sm) |
| 2026-07-10 | 1.0 | Validada 10/10 — GO (7.1 e 7.2 Done). Débito D-721 do gate 7.2 acompanha em commit separado. Status Draft→Ready | Pax (@po) |
| 2026-07-10 | 1.1 | Implementação completa (AC 1–6) + remediação D-721. 294 testes verdes. Status Ready→Ready for Review | Dex (@dev) |
| 2026-07-10 | 1.2 | Gate CONCERNS c/ fixes exigidos: QA-731-1 (modelos sem a coluna) + QA-731-2 (503 sem teste). Volta para @dev | Quinn (@qa) |
| 2026-07-10 | 1.3 | QA-731-1 e QA-731-2 corrigidos; 295 testes verdes. Devolvida para re-review | Dex (@dev) |
| 2026-07-10 | 1.4 | Re-review: PASS (iteração 2). D-721 do gate 7.2 resolvido junto | Quinn (@qa) |
| 2026-07-10 | 2.0 | Close: pre-close gates OK (PASS, ACs 1–6, File List, 295 testes). Status→Done. Épico 7 completo — push com @devops (autorizado pelo dono) | Pax (@po) |
| 2026-07-10 | 2.1 | Merge ff em master + push para origin (`91344bb..6438d91`) | Gage (@devops) |
