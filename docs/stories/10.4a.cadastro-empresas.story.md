# Story 10.4a — Cadastro de empresas e vínculo médico↔empresa

## Status
InReview

## Story
**As a** operador do sistema de cobrança,
**I want** cadastrar empresas (ex.: MEDISA) com dados de cobrança, conta emissora e regra de preço,
e vincular médicos a elas,
**so that** o sistema tenha a base de dados necessária para agregar produção por empresa nas
próximas stories (10.4b — execução agregada, 10.4c — emissão).

## Contexto
Primeira das 3 sub-stories em que a Story 10.4 foi quebrada pelo @architect (ver
`docs/stories/10.4.emissao-por-empresa-medisa.story.md` para o desenho completo e a justificativa
de reuso). Esta sub-story **só cria a base de dados e o cadastro** — nenhuma execução ou emissão
ainda. Sem ela, 10.4b/10.4c não têm onde buscar os dados da empresa.

**Reuso deliberado** (não reinventar): `DadosCobranca`, `CondicoesCobranca`, `ContaEmissora`,
`RegraPreco`/`RegraPrecoForma` (Story 10.1) são reaproveitados tal como estão — uma empresa usa
exatamente os mesmos tipos e as mesmas regras de validação que já existem para médico.

## Acceptance Criteria
1. **Tipos (`packages/shared`):** nova interface `Empresa` — `id`, `nome`, `cobranca: DadosCobranca
   | null` (reaproveita o tipo do médico — pagador PJ), `contaEmissora: ContaEmissora`,
   `condicoes: CondicoesCobranca | null`, `regraPreco: RegraPreco | null`, `ativo`, `createdAt`,
   `updatedAt`. Nenhum tipo reaproveitado é alterado.
2. **Vínculo:** `Medico` ganha `empresaGrupoId: string | null` (FK opcional para `empresas.id`).
   Backfill: todos os médicos existentes com `null` (nenhum comportamento muda). Este campo **não**
   substitui nada do médico — ele continua com `statusHapvida`, `modoCobranca`, etc., para a
   produção individual dele.
3. **Migration:** tabela `empresas` (colunas achatadas, mesmo padrão de `medicos` para
   cobrança/condições/regra de preço — reaproveitar literalmente os mesmos nomes de coluna
   `regra_preco_*` e a mesma CHECK de coerência por forma da migration 0025/0027, só que na tabela
   `empresas`); `medicos.empresa_grupo_id uuid null references empresas(id)`; tabela
   `empresas_historico` (mesmo padrão de `medicos_historico` — toda escrita em empresa gera
   histórico com autor e motivo, PRD §7).
4. **Zod:** `novaEmpresaSchema`/`atualizarEmpresaSchema` reaproveitando diretamente
   `dadosCobrancaSchema`, `condicoesCobrancaSchema`, `regraPrecoSchema` já existentes (não
   duplicar validação).
5. **Repository:** `empresa-repository.ts` — `criarEmpresa`, `listarEmpresas`, `buscarEmpresa`,
   `atualizarEmpresa` (com histórico, motivo obrigatório — mesmo padrão de `medico-repository.ts`).
6. **Rotas:** `GET/POST /api/empresas`, `GET/PATCH/DELETE /api/empresas/[id]`,
   `GET /api/empresas/[id]/historico` — mesmo padrão de auth (`requireRole(['admin'])` para
   escrita) e tratamento de erro das rotas de médico.
7. **UI:** `EmpresaForm.tsx` + `EmpresasManager.tsx` (nova tela em `/empresas` ou dentro de
   Configurações) reaproveitando os blocos de "Dados de cobrança" e "Regra de preço própria" já
   existentes em `MedicoForm.tsx` (extrair se fizer sentido, mas duplicar é aceitável se a extração
   inflar o escopo). `MedicoForm` ganha um campo opcional "Empresa de agrupamento (guias cardíacas)"
   — select das empresas ativas.
8. `npm run typecheck`, `lint`, `test` verdes; testes: Zod (cobrança PJ + regra de preço),
   mappers, repository (CRUD + histórico), componente básico do formulário.

## Complexidade e Riscos
- **Complexidade:** M (~5–6 pontos). É replicar padrões já existentes (médico) para uma entidade
  nova e mais simples (sem `statusHapvida`/`especialidade`/contagem de guias).
- **Escopo OUT:** execução agregada (10.4b); emissão de boleto por empresa (10.4c); qualquer
  cálculo de guias/valor — esta story só cadastra.
- **Riscos:**
  - Confundir `empresa_grupo_id` com `conta_emissora` (Épico 7) — são conceitos ortogonais, não
    reaproveitar a coluna existente (ver Dev Notes da 10.4 pai).
  - Regra de preço da empresa mal configurada não deve travar nada nesta story (só é USADA na
    10.4b) — validar coerência (Zod/CHECK) é suficiente aqui.
- **DoD:** AC 1–8 + testes verdes + File List completa.

## Tasks / Subtasks
- [x] Tipos: `Empresa` em `packages/shared` (AC 1)
- [x] `Medico.empresaGrupoId` (AC 2)
- [x] Migration: tabela `empresas`, `empresas_historico`, `medicos.empresa_grupo_id` (AC 3)
- [x] Zod: schemas de empresa reaproveitando os blocos existentes (AC 4)
- [x] Repository: `empresa-repository.ts` com histórico (AC 5)
- [x] Rotas `/api/empresas` (AC 6)
- [x] UI: `EmpresaForm`/`EmpresasManager` + campo de vínculo em `MedicoForm` (AC 7)
- [x] `typecheck`, `lint`, `test`, `build` (AC 8)

## Dev Notes
- **Reuso concretizado:** `toDadosCobranca`/`toCondicoes`/`toRegraPreco` em `mappers.ts` foram
  generalizadas (parâmetro passou de `MedicoRow` para interfaces estruturais
  `CobrancaRowFields`/`CondicoesRowFields`/`RegraPrecoRowFields`) — `EmpresaRow` as reaproveita sem
  duplicar mapeamento. `empresaUpdateToRow` segue o mesmo padrão de achatamento de
  `medicoUpdateToRow`, mas como função própria (duplicação aceita aqui — os `Object.assign` de
  cobrança/condições/regra são pequenos e a extração não valia o acoplamento extra).
  `novaEmpresaSchema`/`atualizarEmpresaSchema` importam `dadosCobrancaSchema`,
  `condicoesCobrancaSchema`, `regraPrecoSchema`, `contaEmissoraSchema` diretamente de
  `medico-schema.ts` — zero duplicação de regra de validação.
- **`NovaEmpresa` (repository) não inclui `contaEmissora`** no `Pick` — mesmo padrão (e mesma razão)
  de `NovoMedico`: o Zod a torna opcional (ausente → default `'mc'` do banco) e o valor parseado
  chega por tipagem estrutural, não como objeto literal.
- **UI:** `EmpresaForm`/`EmpresasManager` duplicam a estrutura de `MedicoForm`/`MedicosManager`
  (não extraídos) — `EmpresasManager` é bem mais simples (sem sync/import/exclusão em lote).
  `MedicoForm` busca as empresas ativas via `useQuery` (novo) — isso exigiu adicionar
  `QueryClientProvider` ao teste de componente existente (`MedicoForm.test.tsx`), que antes não
  precisava de contexto de react-query.
- **Nav:** entrada "Empresas" adicionada à Sidebar, entre "Médicos" e "Execuções".
- **Não criado `Classe`/faixas/contagem para empresa** — isso é escopo da 10.4b (Engine).

### File List
- `packages/shared/src/types/empresa.ts` (A) — `Empresa`, `EmpresaHistorico`
- `packages/shared/src/types/medico.ts` (M) — `Medico.empresaGrupoId`
- `packages/shared/src/index.ts` (M) — export do módulo `empresa`
- `supabase/migrations/0028_empresas.sql` (A) — tabela `empresas`, `empresas_historico`,
  `medicos.empresa_grupo_id`, RLS (mesmo padrão de `medicos`)
- `apps/web/src/server/repositories/mappers.ts` (M) — `CobrancaRowFields`/`CondicoesRowFields`/
  `RegraPrecoRowFields` (generalização), `EmpresaRow`, `toEmpresa`, `empresaUpdateToRow`,
  `EmpresaHistoricoRow`, `toEmpresaHistorico`, `MedicoRow.empresa_grupo_id`
- `apps/web/src/server/repositories/empresa-repository.ts` (A) — CRUD + histórico + exclusão
  bloqueada por médicos vinculados
- `apps/web/src/server/validation/empresa-schema.ts` (A) — `novaEmpresaSchema`/`atualizarEmpresaSchema`
- `apps/web/src/server/validation/medico-schema.ts` (M) — `empresaGrupoId` em `novoMedicoSchema`/`atualizarMedicoSchema`
- `apps/web/src/app/api/empresas/route.ts` (A) · `[id]/route.ts` (A) · `[id]/historico/route.ts` (A)
- `apps/web/src/services/empresas.ts` (A) · `medicos.ts` (M — `empresaGrupoId` no payload)
- `apps/web/src/components/empresas/EmpresaForm.tsx` (A) · `EmpresasManager.tsx` (A) ·
  `HistoricoTimeline.tsx` (A)
- `apps/web/src/components/medicos/MedicoForm.tsx` (M — campo de vínculo, `useQuery` de empresas)
- `apps/web/src/components/layout/Sidebar.tsx` (M — nav "Empresas")
- `apps/web/src/app/(dashboard)/empresas/page.tsx` (A) · `[id]/historico/page.tsx` (A)
- Testes: `empresa-schema.test.ts` (A), `empresa-repository.test.ts` (A), `EmpresaForm.test.tsx` (A),
  `mappers.test.ts` (M — testes de `toEmpresa`/`empresaUpdateToRow`), `MedicoForm.test.tsx` (M —
  `QueryClientProvider` + mock de `empresasService`)

## Testing
- Vitest. Zod (`empresa-schema.test.ts`): nome obrigatório, cobrança PJ válida/CNPJ×tipo
  incompatível, regra `por_guia` completa/incompleta, `atualizarEmpresaSchema` exige motivo e é
  `strict()`. Mappers: `toEmpresa` monta cobrança/regra a partir das colunas (e null quando
  ausentes); `empresaUpdateToRow` achata e limpa corretamente. Repository (mock Supabase):
  criar/listar/atualizar (histórico só dos campos que mudaram)/excluir (bloqueado por médico
  vinculado)/histórico. Componente: `EmpresaForm` bloqueia salvar sem empresa emissora, "Configurar"
  regra de preço exige taxa na forma `por_guia`, pré-preenche a partir de uma empresa existente.
- `npm run typecheck`, `lint`, `test` e `next build` verdes em todos os workspaces (apps/web 586
  testes, packages/shared 20 testes).

## CodeRabbit Integration
- Story type: **Feature (cadastro)**.
- Foco: reuso correto dos tipos/validações existentes (não duplicar regra), histórico de auditoria
  completo, não confundir `empresa_grupo_id` com `conta_emissora`.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-20 | 0.1 | Story criada pelo @sm a partir do desenho do @architect (Story 10.4) | @sm |
| 2026-07-20 | 0.2 | @po `*validate-story-draft`: GO (checklist 10/10 — título claro, escopo bem definido, dependências mapeadas, riscos documentados, alinhada ao desenho da 10.4). Status → Ready | @po |
| 2026-07-20 | 0.3 | Implementado: `Empresa` (packages/shared), migration 0028, mappers generalizados (Cobranca/Condicoes/RegraPrecoRowFields), `empresa-repository.ts`, rotas `/api/empresas`, `EmpresaForm`/`EmpresasManager`, vínculo em `MedicoForm`, nav na Sidebar. `typecheck`/`lint`/`test`/`build` verdes (586 testes apps/web + 20 shared). Status → InReview | @dev |
