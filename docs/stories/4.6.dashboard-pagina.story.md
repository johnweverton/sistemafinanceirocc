# Story 4.6 — Página do Dashboard financeiro

## Status
Ready for Review

## Story
**As a** gestor do escritório,
**I want** uma página de dashboard com os KPIs financeiros (emitido × recebido × inadimplência) por
competência e por médico, mais o aging de vencidos,
**so that** eu tenha a visão gerencial do ciclo de cobrança em um só lugar.

## Acceptance Criteria
1. Página `/dashboard` (dashboard group) mostra cards de KPI: total emitido, recebido, em aberto,
   vencido e **taxa de inadimplência** da competência selecionada.
2. Seletor de competência; visão **por médico** (tabela) e **aging** (0–30 / 31–60 / 60+).
3. Valores em mono/tabular; badges e estados de carregamento (skeleton) e vazio — design system atual.
4. Consome as agregações da 4.5 via react-query; sem cálculo pesado no cliente.
5. Item "Dashboard" na Sidebar. `typecheck`, `lint`, `test` verdes.

## Complexidade e Riscos
- **Complexidade:** M (~3 pontos). UI de dashboard consumindo agregações prontas.
- **Escopo OUT:** cálculo das agregações (4.5); gráficos avançados (evolução — pode virar follow-up).
- **Riscos:** sem lib de gráficos no projeto — *mitigação:* começar com cards + tabelas + barras
  simples (CSS) para o aging; gráfico rico como evolução (evitar nova dependência sem necessidade).
- **DoD:** AC 1-5 + testes verdes + File List completa.

## Tasks / Subtasks
- [x] Serviço + hooks de dashboard (react-query) consumindo `/api/dashboard/*` (AC: 4)
- [x] Página `apps/web/src/app/(dashboard)/dashboard/page.tsx` + `DashboardManager` (AC: 1-3)
  - [x] Cards de KPI (emitido/recebido/em aberto/vencido/inadimplência); seletor de competência;
        tabela por médico; aging em **barras CSS** (sem lib de gráfico)
  - [x] Skeleton + empty state (reusa `ui/`)
- [x] Item "Dashboard" na Sidebar (AC: 5)
- [x] Testes (AC: 5)
  - [x] componente: KPIs + aging + tabela por médico renderizam; empty state
- [x] `typecheck`, `lint`, `test` (132 verdes)

## Dev Notes
**Depende de 4.5** (agregações).

- **Sem nova dependência de gráfico** salvo necessidade real: usar cards + tabelas + barras em CSS
  (o design system já tem tokens). Se o usuário quiser gráficos ricos depois, vira follow-up com
  @ux-design-expert.
- Reusar `ui/Skeleton`, `ui/EmptyState`, badges e `.tabular` (mono) do Épico 3.
- Padrão de página cliente: `HistoricoExecucoes.tsx` (react-query + service + estados).

### Arquivos afetados
- `apps/web/src/app/(dashboard)/dashboard/page.tsx` (novo) + componentes em `components/dashboard/`
- `apps/web/src/services/dashboard.ts` (novo)
- `apps/web/src/components/layout/Sidebar.tsx` (item nav)
- Testes em `apps/web/tests/components/`

## Testing
- Vitest + Testing Library. Serviço mockado; assert de KPIs e faixas de aging renderizados.

## CodeRabbit Integration
- Story type: **Feature (UI/dashboard)**.
- Foco: acessibilidade, consistência com o design system, sem cálculo no cliente, sem dependência
  desnecessária.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM (a11y) documentar.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-02 | 0.1 | Story criada a partir da arquitetura §6 | River (@sm) |
| 2026-07-02 | 1.0 | Validada 10/10. GO. Status Draft→Ready | Pax (@po) |
| 2026-07-03 | 1.1 | Implementada (página /dashboard + KPIs + aging + tabela por médico). Status Ready→Ready for Review | Dex (@dev) |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (Dex / @dev), modo *develop

### Debug Log References
- Lint warning (useMemo dep instável em `competencias`) → estabilizado com `useMemo(() => comps.data ?? [], [comps.data])`.
- Teste do componente falhou por ambiguidade "Emitido" (KPI + header da tabela) → asserções em labels
  únicos (`Em aberto`/`Inadimplência`) + `getAllByText('Emitido').length >= 2`.

### Completion Notes List
- **Página `/dashboard`** (`DashboardManager`): 5 cards de KPI (emitido/recebido/em aberto/vencido/
  **inadimplência** com destaque vermelho >20%), seletor de competência (KPIs por competência ou soma
  "Todas"), **aging em barras CSS** (reusa `.progress-fill`), tabela por médico (mono + ticket médio).
- **Sem nova dependência de gráfico** (barras via CSS/tokens), conforme a story.
- Reusa `ui/Skeleton`, `ui/EmptyState`, `.tabular`, badges/tokens do design system.
- Item "Dashboard" no topo da Sidebar (visão gerencial).
- **Nota:** médico/aging são globais (ver decisão na 4.5) — o texto "(global)" aparece nos títulos
  dessas seções para deixar claro ao usuário.
- Testes: componente (render KPIs/aging/tabela + empty). Suíte 132 verde. CodeRabbit: pulado (WSL não auth).

### File List
- `apps/web/src/services/dashboard.ts` (A)
- `apps/web/src/components/dashboard/DashboardManager.tsx` (A)
- `apps/web/src/app/(dashboard)/dashboard/page.tsx` (A)
- `apps/web/src/components/layout/Sidebar.tsx` (M) — item Dashboard + DashboardIcon
- `apps/web/tests/components/DashboardManager.test.tsx` (A)

## QA Results

### Gate: PASS ✅ — Quinn (@qa), 2026-07-03

**Escopo:** página `/dashboard` + `DashboardManager` + service react-query + item Sidebar.
Verificação independente: `typecheck` limpo; testes do componente 2/2 verdes; suíte total 132.
CodeRabbit: não executado (WSL não autenticado) — sem CRITICAL/HIGH conhecido; anotado.

**Traceability (AC → evidência):**
- AC1 (cards KPI da competência: emitido/recebido/em aberto/vencido + inadimplência) → `DashboardManager`
  5 KPIs; inadimplência em vermelho quando >20%. ✅
- AC2 (seletor de competência; tabela por médico; aging 0-30/31-60/60+) → seletor + tabela + aging em
  barras CSS. Médico e aging agora filtram por competência (0010). ✅
- AC3 (mono/tabular, badges, skeleton, empty) → `.tabular`, `Skeleton`, `EmptyState` reusados. ✅
- AC4 (consome 4.5 via react-query; sem cálculo pesado no cliente) → 3 `useQuery`; agregação server-side.
  Taxa de inadimplência e KPIs "Todas" vêm da linha de rollup do banco — zero recomputo no cliente. ✅
- AC5 (item Sidebar; gates) → item Dashboard no topo; typecheck/lint/132. ✅

**NFR:**
- **Segurança: PASS.** Dados só via `/api/dashboard/*` (requireRole admin/financeiro); componente não
  acessa DB direto. Sem injeção de HTML (valores formatados via `toLocaleString`).
- **Usabilidade: PASS.** Médico e aging agora respondem ao seletor de competência — escopo indicado
  no subtítulo de cada seção ("· competência 2026-06" / "· todas as competências").

**Observações anteriores (Low) — RESOLVIDAS (migration 0010):**
1. ~~Médico/aging globais (não filtravam por competência)~~ → views recriadas com GROUPING SETS
   (coluna `competencia`). Repositório/rotas aceitam `?competencia`; `DashboardManager` passa `filtro`.
2. ~~`taxaInadimplencia` recomputada no cliente~~ → KPIs "Todas" agora usam a linha de rollup do banco
   (`competencia IS NULL`). `kpi.taxaInadimplencia` vem pronto — nenhum `vencido/emitido` no cliente.

**Veredito:** PASS. Dashboard correto, filtrado, sem recomputo no cliente. Liberado para commit/push.

— Quinn, guardião da qualidade 🛡️

