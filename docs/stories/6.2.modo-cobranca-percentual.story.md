# Story 6.2 — Modo de cobrança por percentual da produção (médicos auxiliares)

## Status
Done

## Story
**As a** operador do sistema de cobrança,
**I want** configurar médicos auxiliares com cobrança por percentual do valor da produção
mensal (em vez da tabela de faixas por guias),
**so that** o sistema reproduza a regra real da Carmem para médicos de produção esporádica
(hoje 5% sobre a produção do mês), sem cálculo manual por fora.

## Contexto
O motor atual (`precos.ts` + `processarMedico`) conhece um único modo: faixas por quantidade
de guias, por classe (PRD §5.1). Médicos que atuam só como auxiliares têm produção esporádica
e a Carmem cobra deles um percentual do valor produzido no mês — regra trazida pela
coordenação (reunião 2026-07-08).

Os dados para a base de cálculo **já são ingeridos** desde o Épico 5: `ItemProducao` traz
`valorCobradoOrigem` (`charged_val`) e `valorPagoOrigem` (`paid_val`), hoje apenas
informativos (decisão 8 do Épico 5). **Esta story revisa a decisão 8 de forma escopada:** no
modo percentual (e SOMENTE nele), o valor da origem passa a ser base de cálculo; no modo
faixas nada muda (preço segue interno).

## ✅ GATE de negócio — RESPONDIDO (dono, 2026-07-08)
1. **Base = valor COBRADO** (`valorCobradoOrigem` / `charged_val`) — a regra considera o
   valor de produção, não o valor efetivamente pago.
2. **Glosados ENTRAM na base** — coerente com a base ser produção (não pagamento); alinha
   com a decisão 5 do Épico 5 (status da origem nunca filtra).
3. **Percentual é especificidade por médico** — 5% vale para os médicos auxiliares atuais,
   mas NÃO é padrão do sistema: campo configurável por médico, sem default de valor
   (confirma o desenho do AC 1/2).

GATE liberado — nada bloqueia a implementação completa (AC 1–6).

## Acceptance Criteria
1. **Migration (@data-engineer):** em `medicos`, `modo_cobranca text NOT NULL DEFAULT
   'faixa_guias'` (CHECK `IN ('faixa_guias','percentual_producao')`) e `percentual_producao
   numeric(5,2) NULL`; CHECK garantindo `percentual_producao IS NOT NULL AND > 0` quando
   `modo_cobranca = 'percentual_producao'`. Backfill: todos os médicos existentes ficam em
   `'faixa_guias'` (comportamento idêntico ao atual).
2. **Tipos/mappers/Zod:** `Medico.modoCobranca` + `Medico.percentualProducao` em
   `packages/shared`, mappers e schemas de validação das rotas de médicos; percentual
   obrigatório e positivo quando o modo é percentual (espelho da CHECK).
3. **Engine (função pura):** `processarMedico` ramifica por `modoCobranca`:
   - `percentual_producao`: `totalValor = percentual × Σ(base dos itens válidos)` (base
     conforme GATE); ignora classes/faixas; contagem de guias/cirurgias e trava de
     conferência (PRD §5.3) **continuam rodando** (são diagnóstico, não preço);
   - resultado carrega o modo e a memória de cálculo (percentual aplicado + base somada)
     para exibição — abordagem de tipo (campo novo em `ResultadoMedico` vs. subtotal
     sintético) a critério do @dev com typecheck guiando, sem quebrar consumidores atuais;
   - alertas (valores retornados, nunca exceção): item válido com base `null` → alerta
     "item sem valor na origem — base subcontada"; base total = 0 → status `alerta`;
   - `faixa_guias`: zero mudança de comportamento (regressão protegida).
4. **UI cadastro:** `MedicoForm` ganha seletor "Modo de cobrança" (default faixas) + campo
   de percentual visível/obrigatório só no modo percentual; badge/indicação do modo na
   listagem (`MedicosManager`).
5. **UI resultados/conferência:** para médico em modo percentual, a tela de resultados exibe
   modo, percentual, base somada e total (em vez dos subtotais por classe/faixa); telas de
   médicos em modo faixas inalteradas. Emissão de boleto consome `totalValor` como hoje,
   sem mudança.
6. `npm run typecheck`, `lint` e `test` verdes; regressão do modo faixas intacta
   (casos de ouro existentes do engine permanecem passando sem edição).

## Complexidade e Riscos
- **Complexidade:** M (~5 pontos). Engine é função pura fácil de ramificar/testar; o volume
  está em migration + tipos + duas frentes de UI.
- **Escopo OUT:** emissão/recebíveis/dashboard (consomem `totalValor` como antes); alteração
  do modo faixas; percentuais escalonados ou por classe; retroatividade sobre execuções
  passadas (resultado é snapshot — recalcular exige nova execução).
- **Riscos:**
  - ~~GATE de negócio sem resposta~~ — resolvido em 2026-07-08 (ver seção GATE).
  - Item com `valorCobradoOrigem`/`valorPagoOrigem` null subconta a base silenciosamente —
    *mitigação:* alerta obrigatório do AC 3 (o sistema nunca chuta, PRD §2).
  - Mudança de modo entre competências muda o valor sem trilha — *mitigação:*
    `medico_historico` já registra alterações de cadastro; conferir que os campos novos
    entram no histórico.
- **DoD:** AC 1–6 + GATE respondido e registrado + testes verdes + File List completa.

## Tasks / Subtasks
- [x] Levar as 3 perguntas do GATE à Carmem/coordenação e registrar respostas (dono/@po) — respondido 2026-07-08
- [x] Migration `medicos` (modo + percentual + CHECKs + backfill) (AC: 1) — `0018_modo_cobranca.sql`
- [x] Tipos/mappers/Zod + histórico de alterações cobrindo campos novos (AC: 2) — histórico
      automático via diff de `atualizarMedico` (campos top-level)
- [x] Engine: ramo percentual em `processarMedico` + alertas de base (AC: 3)
  - [x] fixtures e casos de ouro novos (base cheia, base parcial null, base zero, glosados
        conforme GATE) — 8 casos em `processar-medico-percentual.test.ts`
  - [x] regressão: suíte atual do modo faixas passa sem edição (só a fixture de tipo do
        fake-deps ganhou os campos novos — completude de tipo, não mudança de comportamento)
- [x] UI cadastro: MedicoForm + listagem (AC: 4)
- [x] UI resultados: exibição do modo percentual (AC: 5)
- [x] `typecheck`, `lint`, `test` (AC: 6) — 247 testes verdes (233 web + 14 shared)

## Dev Notes
- **Engine:** `apps/web/src/server/engine/processar-medico.ts` (ramo novo ANTES do loop de
  classes) e `precos.ts` (intocado — faixas seguem como estão). Coding Standard "Engine sem
  I/O": a base vem dos `itens` já recebidos em `EntradaProcessamentoMedico`, nada de fetch.
- **Itens válidos:** o ramo percentual soma `valorCobradoOrigem` sobre
  `itensValidos(itens).validos` (mesmo filtro da contagem, PRD §5.6) — glosados ENTRAM
  (GATE resposta 2; coerente com a decisão 5 do Épico 5: status da origem nunca filtra).
- **Tipos:** `Medico` em `packages/shared/src/types/medico.ts`; `ItemProducao` em
  `packages/shared/src/types/integracao.ts` (campos `valorCobradoOrigem`/`valorPagoOrigem`).
  Atualizar o comentário da decisão 8 no `integracao.ts` referenciando esta story.
- **Histórico:** `medico_historico` (mudança de modo/percentual precisa aparecer lá —
  trilha de auditoria de preço).
- **UI:** `apps/web/src/components/medicos/MedicoForm*` / `MedicosManager.tsx`; resultados em
  `apps/web/src/components/execucoes/` (RelatorioGrupos e afins — localizar onde subtotais
  são renderizados).

### Arquivos afetados (estimativa)
- `supabase/migrations/00XX_modo_cobranca.sql` (A — numeração na implementação)
- `packages/shared/src/types/medico.ts` (M) · `integracao.ts` (M — comentário decisão 8)
- `packages/shared/src/engine-contracts/` (M — `ResultadoMedico`/entrada, conforme abordagem)
- `apps/web/src/server/engine/processar-medico.ts` (M)
- `apps/web/src/server/repositories/{medico-repository,mappers}.ts` (M) + rotas de médicos (M)
- `apps/web/src/components/medicos/*` (M) · componentes de resultados (M)
- Testes: engine (novos casos de ouro), mappers, rotas, componentes

## Testing
- Vitest. Prioridade máxima no engine (função pura): casos de ouro do modo percentual sobre
  `valorCobradoOrigem` (incl. itens glosados na base e itens com valor null → alerta);
  propriedade central —
  médico em `faixa_guias` produz resultado byte-idêntico ao atual (regressão).

## CodeRabbit Integration
- Story type: **Feature (engine de preço/cadastro)**.
- Foco: regressão do modo faixas, coerência CHECK banco ↔ Zod, base null subcontando sem
  alerta, histórico de auditoria dos campos novos.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-08 | 0.1 | Story criada a partir da demanda da coordenação (reunião 2026-07-08); GATE de negócio registrado | River (@sm) |
| 2026-07-08 | 0.2 | GATE respondido pelo dono: base = valor cobrado; glosados entram; 5% é especificidade por médico (não padrão). Story destravada por completo | River (@sm) |
| 2026-07-08 | 1.0 | Validada 10/10 — GO (GATE verificado como respondido e registrado). Status Draft→Ready | Pax (@po) |
| 2026-07-08 | 1.1 | Implementação completa (AC 1–6). 247 testes verdes. Status Ready→Ready for Review | Dex (@dev) |
| 2026-07-08 | 1.2 | Gate PASS (2 LOW documentados, sem bloqueio) | Quinn (@qa) |
| 2026-07-08 | 2.0 | Push para origin. Gates: typecheck/lint/247 testes/build + QA PASS. Status → Done | Gage (@devops) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-5 (Dex / @dev), modo interativo

### Debug Log References
- Decisão de desenho (prevista no AC 3): a memória de cálculo viaja num **subtotal sintético**
  `{classe: 'PERCENTUAL_PRODUCAO', valor: total, faixa: '5% × R$1750.50 (produção cobrada)'}` —
  `Subtotal.classe` alargado para `Classe | 'PERCENTUAL_PRODUCAO'` SEM tocar em `Classe`
  (que é chave de `TabelaPreco = Record<Classe, ...>` — estender o union quebraria a tabela).
  Persistência de graça: `subtotais` já é jsonb em `execucao_resultados` → zero migration nos
  resultados e a tela de conferência exibe modo/percentual/base/total.

### Completion Notes List
- **AC1 (migration):** `0018_modo_cobranca.sql` — `modo_cobranca` default `'faixa_guias'`
  (backfill automático) + `percentual_producao numeric(5,2)`; CHECKs nomeadas e idempotentes
  (modo válido; percentual obrigatório e > 0 no modo percentual). Comments com as decisões
  do GATE.
- **AC2 (tipos/validação/histórico):** `ModoCobranca` + campos em `Medico`; `MedicoRow`
  opcional (`??` com default seguro pré-migration, padrão do `external_id`);
  `medicoUpdateToRow` cobre criar E atualizar (criarMedico reusa o mapper). Zod:
  `modoCobrancaSchema` + refine `percentualCoerente` espelhando a CHECK nos schemas de
  criação e atualização (percentual 0.01–100, 2 casas). **Histórico automático:** o diff de
  `atualizarMedico` opera sobre campos top-level → mudanças de modo/percentual entram em
  `medicos_historico` sem código novo.
- **AC3 (engine):** ramo percentual em `processarMedico` ANTES do loop de classes; base =
  Σ `valorCobradoOrigem` dos itens válidos (GATE: cobrado; glosados entram — status nunca
  filtra); arredondamento a centavos (`Math.round(base × percentual) / 100`); contagem de
  guias/cirurgias e trava de conferência (PRD §5.3) continuam rodando como diagnóstico.
  Alertas (nunca exceção): percentual ausente (defesa em profundidade — a CHECK impede, o
  engine não confia), item sem valor (base SUBCONTADA), base zerada. Modo faixas: código
  intocado dentro do `else`.
- **AC4 (UI cadastro):** MedicoForm — seletor "Modo de cobrança" + campo percentual (visível
  e obrigatório só no modo percentual, com validação inline e `podeSalvar` travado);
  MedicosManager — badge âmbar "N% produção" na coluna Tipo (o TIPO/classe não define preço
  nesse modo).
- **AC5 (UI resultados):** RelatorioGrupos — subtotal sintético renderizado com label
  "% da produção", célula de guias "—" e memória de cálculo na coluna faixa. Emissão de
  boleto inalterada (consome `totalValor`).
- **AC6:** typecheck ✅ · lint ✅ · **247 testes verdes** (233 web + 14 shared; +17 novos:
  8 engine, 7 schema, +2 ajustes). Regressão faixas: casos de ouro existentes
  (precos/contagem/regressao-prd12) passaram SEM edição.
- Comentário da decisão 8 atualizado em `integracao.ts` referenciando a revisão escopada.
- CodeRabbit self-healing: pulado (CLI WSL não autenticado — estado conhecido).
- **Migration 0018 pendente de aplicação manual no Supabase pelo dono** (conta externa).

### File List
- `supabase/migrations/0018_modo_cobranca.sql` (A)
- `packages/shared/src/types/medico.ts` (M) — `ModoCobranca` + campos em `Medico`
- `packages/shared/src/types/execucao.ts` (M) — `Subtotal.classe` alargado
- `packages/shared/src/types/integracao.ts` (M) — comentário da decisão 8 (revisão escopada)
- `packages/shared/src/engine-contracts/index.ts` (M) — Pick com campos novos
- `apps/web/src/server/repositories/mappers.ts` (M) — MedicoRow/toMedico/medicoUpdateToRow
- `apps/web/src/server/engine/processar-medico.ts` (M) — ramo percentual
- `apps/web/src/server/validation/medico-schema.ts` (M) — modoCobrancaSchema + refine
- `apps/web/src/services/medicos.ts` (M) — NovoMedicoPayload
- `apps/web/src/components/medicos/MedicoForm.tsx` (M) — seletor + campo percentual
- `apps/web/src/components/medicos/MedicosManager.tsx` (M) — badge do modo
- `apps/web/src/components/execucoes/RelatorioGrupos.tsx` (M) — render do subtotal sintético
- `apps/web/tests/server/engine/processar-medico-percentual.test.ts` (A) — 8 casos de ouro
- `apps/web/tests/server/validation/medico-schema-percentual.test.ts` (A) — 7 casos Zod
- `apps/web/tests/server/orchestrator/fake-deps.ts` (M) — fixture com campos novos

## QA Results

### Review — Quinn (@qa) — 2026-07-08 — commit `67bbb93`

**Gate: PASS** (2 observações LOW documentadas, sem bloqueio)

**Evidência independente:** typecheck ✅ · lint ✅ · **247 testes verdes** (233 web + 14 shared)
re-executados nesta sessão; escopo do commit confere com a File List. CodeRabbit pulado
(CLI WSL não autenticado — estado conhecido).

**Rastreabilidade dos AC:**

| AC | Verificação |
|----|-------------|
| 1 (migration) | ✅ 0018 idempotente (add if not exists + CHECKs nomeadas com drop/add); backfill via default; `numeric(5,2)` comporta o range validado (0.01–100); comments registram o GATE |
| 2 (tipos/Zod/histórico) | ✅ refine `percentualCoerente` espelha a CHECK em criação E atualização; `MedicoRow` opcional com default seguro pré-migration (padrão external_id); histórico automático verificado — diff de `atualizarMedico` cobre campos top-level |
| 3 (engine) | ✅ base = Σ valorCobradoOrigem dos válidos (GATE: cobrado, glosados entram — teste de ouro cobre valor pago ≠ cobrado); arredondamento a centavos CONFERIDO (caso 87.525→87.53); conferência/contagem seguem rodando; 3 alertas defensivos; modo faixas intocado no `else` |
| 4 (UI cadastro) | ✅ seletor + campo condicional com validação inline e submit travado; badge na listagem; form sempre envia modo+percentual juntos (payload completo) |
| 5 (UI resultados) | ✅ subtotal sintético com label "% da produção" e memória de cálculo na coluna faixa; emissão consome totalValor inalterado (resultado com base zerada vira 'alerta' → guard de emissão já bloqueia) |
| 6 (validações) | ✅ independentes; regressão: casos de ouro (precos/contagem/regressao-prd12) sem edição — única mudança em teste existente é completude de TIPO na fixture do fake-deps (aceitável) |

**Positivos:** a decisão do subtotal sintético é elegante — memória de cálculo persiste no
jsonb existente (zero migration em resultados, snapshot histórico auditável por execução);
defesa em profundidade correta (Zod → CHECK → engine não confia e alerta); revisão da
decisão 8 do Épico 5 documentada NO código (`integracao.ts`), não só na story.

#### Issues LOW (registrados, sem ação obrigatória)
- **L-1** Via API direta (não pela UI), update parcial que envia `percentualProducao: null`
  sem `modoCobranca` num médico JÁ em modo percentual passa no Zod e estoura na CHECK do
  banco → 500 em vez de 422. A UI nunca produz esse payload (form envia sempre os dois
  campos) e a defesa final existe — melhorar a mensagem numa revisão futura do schema.
- **L-2** Observação operacional: a trava de variação anômala (PRD §8.5) tende a disparar
  com frequência para auxiliares (produção esporádica por natureza) → resultados 'alerta'
  exigirão revisão manual recorrente. Comportamento ESPECIFICADO no AC 3 (diagnóstico
  roda) — monitorar o ruído em produção; se atrapalhar a operação, tunar a trava para o
  modo percentual em story futura.

#### Riscos operacionais (dono — fora do código)
1. **Migration `0018` precisa ser aplicada manualmente no Supabase ANTES do deploy** (junto
   com a `0017` da story 6.1, ainda pendente) — sem ela, salvar médico com modo percentual
   falha (coluna inexistente).
2. Configurar os médicos auxiliares reais (modo + 5%) e validar a primeira execução
   comparando com o cálculo manual da Carmem.

**Veredito: PASS.** Handoff ao @devops para push do commit `67bbb93`.
