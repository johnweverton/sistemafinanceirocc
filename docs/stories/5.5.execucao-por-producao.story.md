# Story 5.5 — Execução por produção (seleção manual) + cutover do contrato presumido

## Status
Done

## Story
**As a** operador do sistema de cobrança,
**I want** iniciar a execução escolhendo manualmente a produção de cada médico (listada da API
real) e ter o processamento rodando sobre `fin-itens`,
**so that** a contagem/valores venham direto da origem — e o código do contrato presumido
(que nunca existiu em produção) seja removido.

## Acceptance Criteria
1. **Tela Nova Execução** (arquitetura §3.5): informa competência; lista médicos **ativos,
   configurados e vinculados** com dropdown das produções da origem
   (`listarProducoes` por médico, concorrência limitada ~8);
   - pré-seleção automática quando o nome casa com a competência de forma inequívoca
     ("Janeiro 2026" ↔ `2026-01`), **sempre editável** (decisão 7);
   - médico sem produção selecionada fica fora (vira `sem_dados`);
   - médicos com `necessitaConfiguracao=true` ou sem vínculo aparecem em seção separada com
     aviso explícito ("fora da execução — completar cadastro/vincular"), linkando para pendências (5.4).
2. `POST /api/execucoes` aceita `selecoes: [{ medicoId, producaoExternaId, producaoNome }]`;
   valida (médico ativo/configurado/vinculado, sem duplicata) e persiste em `execucao_selecoes`
   (0011) no início; `total_medicos` = nº de seleções.
3. **Orchestrator:** processa por seleção — `buscarItens(producaoExternaId)` (5.1) +
   `contarGuiasProducao`/`detectarModoProducao` (5.3) + preços internos (`precos.ts`, decisão 8);
   `guiasExecucaoAnterior` passa a consultar por `medico_id` (índice da 0011); resultado grava
   `cpf` como snapshot (pode ser null); falha de rede num médico vira alerta sem derrubar o lote
   (comportamento atual preservado); lotes encadeados (BATCH_SIZE/X-Internal-Secret) mantidos.
4. **Cutover — remoção do contrato presumido:** `procedimentos-client.ts` (caminho http),
   `listarCpfsComProcedimentos`, a descoberta silenciosa em `iniciarExecucao`, o tipo
   `Procedimento` e as fixtures antigas são **removidos**; funções antigas de `contagem.ts`
   removidas ou adaptadas para o fluxo novo (sem código morto); env `CARMEM_API_URL`/
   `CARMEM_API_KEY`/`PROCEDIMENTOS_SOURCE` saem do schema (fica só `FIN_API_SOURCE`).
5. Tela de resultados/conferência continua funcionando (procedimentos, cirurgias, guias,
   consolidado, alertas, subtotais/valores) sem regressão visual ou de dados.
6. `npm run typecheck`, `lint` e `test` passam; suíte do orchestrator adaptada à nova assinatura
   de deps (fake-deps atualizados); suíte existente permanece verde.

## Complexidade e Riscos
- **Complexidade:** L/XL (~8 pontos). Toca orchestrator, rotas, UI de execução e faz a limpeza.
  Se ficar grande na prática, quebrar em 5.5a (orchestrator+rota) e 5.5b (tela+cutover) com o @sm.
- **Escopo OUT:** emissão de boletos, recebíveis, dashboard (inalterados — consomem
  `execucao_resultados` como antes).
- **Riscos:**
  - **GATE EXTERNO:** rodar em produção **preferencialmente após** o programador entregar o campo
    de atendimento no `fin-itens` (pendência externa do épico) — o fallback (paciente, data)
    subconta guias quando o mesmo paciente tem 2 atendimentos no mesmo dia (caso confirmado pelo
    dono). O código já está pronto para o campo (5.1/5.3); o gate é operacional, não bloqueia dev.
  - Remoção do tipo `Procedimento` tem ripple amplo — *mitigação:* typecheck guia; fazer por último.
  - N chamadas `listarProducoes` na tela — *mitigação:* concorrência limitada + volume ~120.
- **DoD:** AC 1-6 + testes verdes + File List completa.

## Tasks / Subtasks
- [x] Rota `POST /api/execucoes` com `selecoes` + persistência em `execucao_selecoes` (AC: 2)
  - [x] mapper/repositório para `execucao_selecoes` (padrão service role)
- [x] Orchestrator por seleção (AC: 3)
  - [x] `OrchestratorDeps`: trocar `buscarProcedimentos(cpf, competencia)` por
        `buscarItens(producaoExternaId)`; cursor de lote sobre as seleções
  - [x] `guiasExecucaoAnterior(medicoId, competencia)`
  - [x] remover fase de descoberta silenciosa
- [x] Tela Nova Execução (AC: 1)
  - [x] endpoint de apoio para montar a tela (médicos vinculados + produções da origem)
  - [x] pré-seleção por casamento de nome (função pura testável) + seção de excluídos com aviso
- [x] Conferência/resultados sem regressão (AC: 5)
- [x] Cutover (AC: 4) — por último, com typecheck guiando
- [x] Testes (AC: 6): orchestrator com fake-deps novos; casamento de nome; rota com validações
- [x] `typecheck`, `lint`, `test` (165 verdes: 151 web + 14 shared)

## Dev Notes
**Pré-requisitos:** stories 5.2 e 5.3 concluídas; migration `0011` aplicada
(`execucao_selecoes`, índice `idx_execucao_resultados_medico`).

- **Orchestrator atual:** `apps/web/src/server/orchestrator/execucao-orchestrator.ts` — manter o
  desenho de lotes encadeados e o isolamento de falha por médico (`processarUmMedico` → alerta).
  Testes de referência: `apps/web/tests/server/orchestrator/` (fake-deps injetáveis).
- **Casamento nome↔competência:** mapa mês pt-BR → `MM` ("janeiro"→01…); casa se o nome
  normalizado contém `{mês} {ano}` da competência; ambíguo/nada → dropdown sem pré-seleção.
- **`processarMedico` (engine):** hoje recebe `Procedimento[]`; passa a receber `ItemProducao[]` e
  usar as funções da 5.3 — subtotais/classes/preços internos inalterados (decisão 8).
- **Invariante da 0005:** o filtro `ativo AND NOT necessita_configuracao` do orquestrador é
  preservado — a novidade é só o AVISO explícito na tela (observação da @data-engineer na 0011).
- Snapshot de seleções é imutável após o início (auditoria) — sem UPDATE em `execucao_selecoes`.

### Arquivos afetados
- `apps/web/src/server/orchestrator/execucao-orchestrator.ts` (M)
- `apps/web/src/server/repositories/{execucao-repository,mappers}.ts` (M)
- `apps/web/src/app/api/execucoes/route.ts` (M) + endpoint de apoio da tela (A)
- UI de execuções (tela nova/ajustada) em `apps/web/src/app/(app)/execucoes/` e componentes
- **Removidos:** `procedimentos-client.ts` (http), fixtures antigas, `Procedimento`,
  funções antigas de `contagem.ts`, env `CARMEM_*`/`PROCEDIMENTOS_SOURCE`
- Testes em `apps/web/tests/server/orchestrator/` e afins

## Testing
- Vitest. Orchestrator 100% com fake-deps (sem rede/DB); funções puras da pré-seleção com casos
  pt-BR; rota com validação de payload. Smoke manual do fluxo com `FIN_API_SOURCE=local`.

## CodeRabbit Integration
- Story type: **Feature (orchestration/cutover)**.
- Foco: regressão no encadeamento de lotes, remoção completa sem código morto, validação da rota.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-06 | 0.1 | Story criada a partir da arquitetura §3.5/§5.3/§9 + decisão 7 do épico | River (@sm) |
| 2026-07-06 | 1.0 | Validada 10/10. GO (gate externo do cutover registrado nos Riscos). Status Draft→Ready | Pax (@po) |
| 2026-07-06 | 1.1 | Implementação original commitada fora do fluxo (commits f13e0a1–cf9e9dc, sem registro) | — (externo) |
| 2026-07-06 | 1.2 | Gate FAIL: H-1/H-2 + M-1/M-2/M-3 + LOWs | Quinn (@qa) |
| 2026-07-06 | 1.3 | Fixes do gate aplicados (todos). 165 testes verdes. Status Ready→Ready for Review | Dex (@dev) |
| 2026-07-06 | 2.0 | Push para origin (gates: typecheck/lint/165 testes/build + QA PASS). Status → Done | Gage (@devops) |

## Dev Agent Record
### Agent Model Used
claude-fable-5 (Dex / @dev), modo *apply-qa-fixes

### Debug Log References
- **Situação atípica documentada:** a implementação original desta story (tela Nova Execução,
  orchestrator por seleção, cutover) foi commitada e pushada FORA do fluxo AIOS (commits
  `f13e0a1`, `d67fad5`, `fd5c2ad`, `cf9e9dc`), sem Dev Agent Record, checkboxes ou gate prévio.
  Este registro cobre os FIXES do gate FAIL do @qa (2026-07-06) aplicados sobre esse código.

### Completion Notes List
- **H-1 (dupla contagem):** `listarSelecoes` agora ordena por `medico_id` (chave única) —
  o cursor de lote (`slice`) nunca duplica/pula médico entre lotes. Comentário explica o porquê.
- **H-2 (regressão esvaziada):** `regressao-prd12.test.ts` reescrito limpo — asserção de
  `detectarModo='sim'` (Dra. A) e `'nao'` (Dr. E) restauradas via `atendimentoExternoId` das
  fixtures; +1 teste garantindo que cadastro sim × observado sim NÃO gera alerta falso;
  comentários de raciocínio de IA removidos.
- **M-1 (validação server-side):** `iniciarExecucao` valida as seleções antes de criar:
  duplicado → 422 `SELECAO_DUPLICADA`; inexistente/inativo/`necessitaConfiguracao`/sem
  `externalId` → 422 `SELECAO_INVALIDA` com lista `invalidos`. Novo `listarMedicosPorIds`
  (1 query `.in()`) no medico-repository; dep nova no orchestrator + fake-deps.
- **M-2 (execução zumbi):** `criarExecucao` marca a execução como `erro` se o insert de
  `execucao_selecoes` falhar (não fica presa em 'processando').
- **M-3 (detecção de modo cega):** `detectarModoProducao` agrupa por
  `atendimentoExternoId ?? pacienteNome` (chave SEM data) — detecta mudança de data no fallback
  por paciente (arquitetura §3.3) e por atendimento quando a origem entregar o campo. Testes
  ajustados (+2 casos: pacientes distintos → nao; atendimentos distintos multi-data → nao).
- **LOWs:** `apiFetch<SyncRelatorio>` no service, `useState<SyncRelatorio | null>` no
  MedicosManager, cast tipado `ExecucaoSelecaoRow[]` no repositório; `supabase/.temp/` no
  `.gitignore` + removido do índice (`git rm --cached`).
- **Divergência de rotas (LOW do gate) NÃO alterada:** `/api/sync/medicos*` mantidas — mudar
  rota é decisão de documentação (@po/@architect atualizam stories 5.2/5.4) e não de código.
- Validações: typecheck ✅ · lint ✅ · **165 testes verdes** (151 web + 14 shared; +6 novos).
- CodeRabbit self-healing: pulado (CLI WSL não autenticado — estado conhecido).

### File List
- `apps/web/src/server/repositories/execucao-repository.ts` (M) — H-1 order medico_id, M-2 marca erro, cast tipado
- `apps/web/src/server/repositories/medico-repository.ts` (M) — `listarMedicosPorIds`
- `apps/web/src/server/orchestrator/execucao-orchestrator.ts` (M) — M-1 validação de seleções + dep nova
- `apps/web/src/server/engine/contagem-producao.ts` (M) — M-3 detecção por atendimento/paciente
- `apps/web/tests/server/engine/regressao-prd12.test.ts` (M) — H-2 reescrito com asserções
- `apps/web/tests/server/engine/contagem-producao.test.ts` (M) — casos de modo ajustados (+2)
- `apps/web/tests/server/orchestrator/fake-deps.ts` (M) — `listarMedicosPorIds` + externalId default
- `apps/web/tests/server/orchestrator/orchestrator-unit.test.ts` (M) — +2 testes de validação
- `apps/web/src/services/medicos.ts` (M) — `apiFetch<SyncRelatorio>`
- `apps/web/src/components/medicos/MedicosManager.tsx` (M) — tipo `SyncRelatorio`
- `.gitignore` (M) + `supabase/.temp/*` (D do índice)

## QA Results

### Review — Quinn (@qa) — 2026-07-06

**Gate: FAIL** (2 HIGH — correções pontuais; retornar ao @dev)

**Contexto atípico:** o código do cutover foi commitado e pushado fora do fluxo (commits
`fcbdaaf`…`cf9e9dc`) sem Dev Agent Record, sem checkboxes e sem passar por este gate antes do
push — lacuna de processo registrada. Este review foi feito diretamente sobre o código commitado.

**Evidência:** typecheck ✅ · lint ✅ · 159 testes verdes (145 web + 14 shared) verificados de
forma independente nesta sessão. Cutover completo: `Procedimento`, `procedimentos-client.ts`,
descoberta por CPF e env `CARMEM_*`/`PROCEDIMENTOS_SOURCE` removidos sem referências mortas.

**AC:** AC3 ✅ (orchestrator por seleção, `guiasExecucaoAnterior` por `medico_id`, falha isolada
por médico, lotes preservados) · AC4 ✅ · AC6 ✅ · AC1 parcialmente inspecionado (tela existe;
build/testes verdes) · **AC2 parcialmente atendido** (persistência ✅; validação server-side ✗ —
ver M-1) · AC5 enfraquecido (ver H-2).

#### Issues HIGH (bloqueiam)
- **H-1 [orchestrator/repo] Cursor de lote sobre ordenação instável — risco de dupla contagem.**
  `listarSelecoes` ordena por `producao_nome`, que é praticamente idêntico para todas as seleções
  ("Janeiro 2026" ×120). Ordem de empate no Postgres NÃO é determinística; o cursor
  `slice(jaProcessados, +batchSize)` refaz a query a cada lote → médico pode ser processado 2×
  (resultado duplicado = cobrança dobrada) ou pulado. **Fix:** `order('medico_id')` (ou `id`),
  chave única e estável. Uma linha em `execucao-repository.listarSelecoes`.
- **H-2 [testes] Regressão PRD §12 com teste sem asserção.** Em `regressao-prd12.test.ts`, o
  teste "detecta modo observado = sim" tem o corpo composto só de comentários (zero expects) e o
  1º teste contém raciocínio de IA não limpo. A trava de modo (PRD §5.3) perdeu cobertura de
  regressão silenciosamente — o arquivo passa verde sem testar. **Fix:** restaurar asserções
  usando `atendimentoExternoId` na fixture (única forma de multi-data por grupo) e remover os
  comentários soltos.

#### Issues MEDIUM (corrigir ou registrar como débito)
- **M-1 [rota/orchestrator] Sem validação server-side da seleção (AC2).** `iniciarExecucao`
  aceita qualquer `medicoId`: inativo, `necessitaConfiguracao=true` ou sem vínculo é processado
  se a UI enviar. O invariante da 0005 hoje só é garantido pela UI. **Fix:** validar cada seleção
  no início (422 com lista de inválidos).
- **M-2 [repo] `criarExecucao` não-transacional.** Insere `execucoes` e depois `execucao_selecoes`;
  falha na 2ª (ex.: `medicoId` duplicado no payload → UNIQUE 0011) deixa execução zumbi em
  'processando' para sempre. **Fix mínimo:** deduplicar/validar antes + marcar erro na execução
  se o insert de seleções falhar.
- **M-3 [engine] `detectarModoProducao` cego sob o fallback.** Agrupa por `chaveAtendimento`, que
  no fallback JÁ contém a data → grupo nunca tem 2 datas → modo observado é sempre 'nao' até a
  origem entregar o campo de atendimento. Consequência: pediatra cadastrado `modoMudancaData='sim'`
  receberá alerta falso "MODO INCONSISTENTE" em toda execução. Arquitetura §3.3 especificava
  detecção por PACIENTE (>1 data do mesmo paciente). Direção segura (gera alerta, não subcobra),
  mas ruidoso e divergente da spec. **Fix:** detectar por `pacienteNome`.

#### Issues LOW (débito documentado)
- Rotas implementadas em `/api/sync/medicos*` divergem da spec das stories 5.2/5.4
  (`/api/medicos/sincronizar|vincular|criar-externo`) — atualizar stories/arquitetura ou rotas.
- Tipos `any`: `services/medicos.ts` (`sincronizar: apiFetch<any>`), `MedicosManager`
  (`syncRelatorio: any`), `listarSelecoes` (`data as any[]`) — tipar com `RelatorioSincronizacao`.
- `supabase/.temp/` versionado — adicionar ao `.gitignore` e remover do índice.

#### Riscos operacionais (fora do código — dono)
- Migration `0011` sem confirmação de aplicação no Supabase (MCP sem permissão para verificar).
- Gate externo vivo: campo de atendimento no `fin-itens` pendente com o programador — fallback
  (paciente, data) **subconta guias** no caso confirmado de 2 atendimentos no mesmo dia; validado
  pelo caso de ouro 5g, mas o cutover já está no código: não usar em produção antes do campo.

**Positivos:** os 8 casos de ouro do AC5 da 5.3 estão implementados e corretos (todos os status
contam; via_acesso agrupa; pediatra teto n/3 preservado; subcontagem do fallback documentada em
teste); rotas de sync com `requireRole(['admin'])`; segredos só server-side.

**Veredito: FAIL** — retornar ao @dev para H-1/H-2 (e idealmente M-1..M-3). Re-review após fixes.

### Re-review — Quinn (@qa) — 2026-07-06 (após commit 61760dd)

**Gate: PASS**

Rastreabilidade fix ↔ achado, verificada no diff do commit e no código:

| Achado | Verificação |
|--------|-------------|
| H-1 cursor instável | ✅ `listarSelecoes` ordena por `medico_id` (única/estável); comentário protege contra regressão |
| H-2 regressão esvaziada | ✅ arquivo reescrito; asserções de modo restauradas (Dra. A `sim`, Dr. E `nao`); zero testes sem expect (grep confirmou); +1 teste anti-alerta-falso |
| M-1 validação server-side | ✅ 422 `SELECAO_DUPLICADA`/`SELECAO_INVALIDA` com lista; `listarMedicosPorIds` 1 query; +2 testes (nada criado em falha) |
| M-2 execução zumbi | ✅ falha no insert de seleções marca `status='erro'` |
| M-3 detecção cega | ✅ chave sem data (`atendimentoExternoId ?? paciente`); 3 casos de teste cobrem fallback e campo presente (2 atendimentos multi-data → `nao`) |
| LOWs | ✅ tipos `SyncRelatorio`/`ExecucaoSelecaoRow`; `supabase/.temp/` fora do índice + .gitignore |

- **Evidência independente:** typecheck ✅ · lint ✅ · **165 testes verdes** (151 web + 14 shared) re-executados neste re-review.
- **Débito documental remanescente (LOW, aceito):** rotas `/api/sync/medicos*` divergem da spec
  das stories 5.2/5.4 — ajuste de DOCUMENTAÇÃO com @po/@sm (código mantido de propósito).
- **Riscos operacionais que permanecem com o dono (não são de código):**
  1. Confirmar aplicação da migration `0011` no Supabase antes de usar sync/execução.
  2. Gate externo: campo de atendimento no `fin-itens` pendente com o programador — não rodar
     execução real antes (fallback subconta guias no caso confirmado de 2 atendimentos/dia).

**Veredito: PASS** — handoff ao @devops para push do commit `61760dd`.
