# Story 3.3 — UI: dados de cobrança no MedicoForm + ViaCEP + tela de Configurações

## Status
Ready

## Story
**As a** operador/admin,
**I want** preencher os dados de cobrança do médico (com autofill de endereço por CEP) e
configurar os defaults comerciais do escritório,
**so that** os médicos fiquem prontos para emissão sem digitação manual excessiva nem erro.

## Acceptance Criteria
1. `MedicoForm` ganha uma seção **"Dados de cobrança"** (colapsável) com: tipo (PF/PJ), documento
   (máscara conforme tipo), nome/razão social, e-mail, CEP, logradouro, número, complemento,
   bairro, cidade, UF.
2. Ao completar um CEP válido, os campos logradouro/bairro/cidade/UF são preenchidos via **ViaCEP**
   (`https://viacep.com.br/ws/{cep}/json/`), com tratamento de CEP inexistente.
3. A seção é **opcional**: salvar médico sem cobrança continua funcionando (preserva
   auto-descoberta); campos parciais são validados pelo schema de 3.1 ao enviar.
4. Overrides comerciais por médico (vencimento/multa/juros/desconto) editáveis na mesma seção
   (opcionais; vazio = herda o global).
5. Nova tela/aba **Configurações de Cobrança** (admin) para editar `config_cobranca`
   (dias_vencimento, multa, juros, desconto), lendo/gravando via API.
6. Indicação visual de "cobrança pendente" reaproveita a semântica de `cobrancaCompleta`
   (ex.: badge no card/lista do médico).
7. `typecheck`, `lint`, `test` verdes; segue o design system atual (tokens `cc-*`, tema dark/light).

## Complexidade e Riscos
- **Complexidade:** L (~5 pontos). Maior story: form + máscara + ViaCEP + tela de config + CSP.
- **Escopo OUT:** emissão/gateway (3.2) e importação CSV (3.4).
- **Riscos:**
  - CSP bloquear o fetch do ViaCEP se `https://viacep.com.br` não for adicionado ao `connect-src`.
    *Mitigação:* task explícita de CSP em `next.config.mjs`.
  - Indisponibilidade/timeout do ViaCEP. *Mitigação:* degradar com graça — permitir digitação
    manual do endereço; não bloquear o salvamento.
  - Autorização da tela de Config (só admin). *Mitigação:* `requireRole` no PUT + esconder no nav.
- **DoD:** AC 1-7 + testes verdes + File List completa.
- [ ] Seção de cobrança em `apps/web/src/components/medicos/MedicoForm.tsx` (AC: 1, 3, 4)
  - [ ] Campos controlados + máscara de documento por `pagadorTipo`
  - [ ] Overrides comerciais opcionais
- [ ] Hook/utility de ViaCEP (AC: 2)
  - [ ] `apps/web/src/lib/viacep.ts` (fetch client-side, sem segredo) + tratamento de erro
- [ ] API + tela de Configurações de Cobrança (AC: 5)
  - [ ] Rota `GET/PUT /api/config-cobranca` (requireRole admin no PUT) usando o repositório de 3.1
  - [ ] Página `apps/web/src/app/(dashboard)/configuracoes/page.tsx` + form
  - [ ] Item de navegação na `Sidebar` (somente admin, se aplicável)
- [ ] Indicador de cobrança pendente (AC: 6)
  - [ ] Usar `cobrancaCompleta` no `MedicosManager` (badge)
- [ ] Testes (AC: 7)
  - [ ] `MedicoForm`: renderiza a seção; autofill ViaCEP com `fetch` mockado
  - [ ] Config: form renderiza e envia (mock)
- [ ] `typecheck`, `lint`, `test`

## Dev Notes
**Depende de 3.1** (tipos, `dadosCobrancaSchema`, `cobrancaCompleta`, repositório de config).

- `MedicoForm` atual: `apps/web/src/components/medicos/MedicoForm.tsx` (tem teste em
  `apps/web/tests/components/MedicoForm.test.tsx` — manter verde).
- ViaCEP é público e client-side; **não** adicionar segredo nem rota server para isso. A CSP já
  permite `connect-src 'self'` + Supabase; **adicionar `https://viacep.com.br` ao `connect-src`**
  em `apps/web/next.config.mjs` (senão o fetch é bloqueado pela CSP).
- Design system: usar classes existentes (`.input`, `.btn-primary`, `.card`, `.field-label`,
  `.badge-*`) — ver `apps/web/src/app/globals.css`. Tema dark/light já ativo.
- Máscara: PF → CPF (11), PJ → CNPJ (14). Enviar apenas dígitos ao backend (o schema de 3.1 espera
  dígitos).
- Toasts já existem (`components/ui/Toast.tsx`) — usar para feedback de salvar/erro.

### Arquivos afetados
- `apps/web/src/components/medicos/MedicoForm.tsx`
- `apps/web/src/lib/viacep.ts` (novo)
- `apps/web/src/app/api/config-cobranca/route.ts` (novo)
- `apps/web/src/app/(dashboard)/configuracoes/page.tsx` (novo) + `Sidebar.tsx`
- `apps/web/src/components/medicos/MedicosManager.tsx` (badge)
- `apps/web/next.config.mjs` (CSP connect-src + viacep)
- `apps/web/src/services/*` conforme necessário
- Testes correspondentes

## Testing
- Vitest + Testing Library. Mockar `fetch` do ViaCEP. Não chamar rede real.

## CodeRabbit Integration
- Story type: **Feature (UI/formulário)**.
- Foco: acessibilidade (labels/aria), validação client alinhada ao Zod de 3.1, CSP atualizada,
  consistência com o design system. Severidade: CRITICAL/HIGH auto-fix; MEDIUM (a11y) documentar.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-01 | 0.1 | Story criada a partir da arquitetura §5.1, §6 | River (@sm) |
| 2026-07-01 | 1.0 | Validada (10/10 pós-fix): +complexidade +riscos. GO. Status Draft→Ready | Pax (@po) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
