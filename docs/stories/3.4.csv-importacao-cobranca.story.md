# Story 3.4 — Importação CSV estendida com dados de cobrança

## Status
Ready

## Story
**As a** operador,
**I want** importar médicos por CSV já trazendo os dados de cobrança (quando disponíveis),
**so that** o cadastro em lote deixe os médicos prontos para emissão sem retrabalho manual.

## Acceptance Criteria
1. `parseCsv`/`rowToInput` de `/api/medicos/importar` reconhecem as colunas de cobrança:
   `pagador_tipo`, `pagador_documento`, `pagador_nome`, `email`, `cep`, `logradouro`, `numero`,
   `complemento`, `bairro`, `cidade`, `uf`.
2. As colunas de cobrança são **opcionais**: linha sem elas importa o médico com cobrança vazia
   (comportamento atual preservado).
3. Se dados de cobrança forem fornecidos parcialmente/invalidamente, a linha é rejeitada com
   mensagem clara (linha + campo), sem abortar o lote (padrão atual de `erros[]`).
4. O modelo `public/templates/medicos-modelo.csv` é atualizado com as novas colunas e um exemplo
   preenchido e um sem cobrança.
5. Validação reusa `dadosCobrancaSchema` de 3.1 (documento por tipo, cep, uf, email).
6. Limites de segurança atuais (5 MB / 5000 linhas) permanecem.
7. `typecheck`, `lint`, `test` verdes.

## Complexidade e Riscos
- **Complexidade:** S (~2 pontos). Extensão pontual de parsing + modelo + testes.
- **Escopo OUT:** UI de cobrança (3.3); emissão (3.2).
- **Riscos:**
  - Vírgula em campos de endereço (ex.: complemento) quebra o `parseCsv` por split simples.
    *Mitigação:* decisão a registrar (orientar no modelo a evitar vírgula **ou** parser com aspas)
    — o dev escolhe o menor risco e documenta no Change Log.
- **DoD:** AC 1-7 + testes verdes + File List completa.
- [ ] Estender parsing em `apps/web/src/app/api/medicos/importar/route.ts` (AC: 1, 2, 3, 5)
  - [ ] `rowToInput` monta o bloco de cobrança quando presente
  - [ ] Integrar validação de cobrança (schema de 3.1) por linha; erro entra em `erros[]`
- [ ] Atualizar `apps/web/public/templates/medicos-modelo.csv` (AC: 4)
- [ ] Testes (AC: 2, 3, 7)
  - [ ] Linha sem cobrança → importa (cobrança nula)
  - [ ] Linha com cobrança válida (PF e PJ) → importa com bloco
  - [ ] Linha com cobrança inválida (documento/cep/uf) → entra em `erros[]`, lote continua
- [ ] `typecheck`, `lint`, `test`

## Dev Notes
**Depende de 3.1** (schema e tipos de cobrança; `criarMedico` deve aceitar o bloco).

- Arquivo atual: `apps/web/src/app/api/medicos/importar/route.ts` — `parseCsv` é um split simples
  por vírgula (linha 9-20) e `rowToInput` mapeia colunas (linha 22-34). **Atenção:** endereços
  podem conter vírgula (ex.: complemento) — decidir e documentar a convenção (recomendado: manter
  o split simples e orientar no modelo que campos com vírgula devem ser evitados **ou** implementar
  parsing com aspas; escolher o menor risco e registrar no Change Log).
- Reusar `novoMedicoSchema` já estendido em 3.1 (não duplicar validação).
- Limites anti-DoS já existem (`MAX_CSV_BYTES`, `MAX_CSV_ROWS`) — não remover.
- Só admin importa (`requireRole(['admin'])`) — inalterado.

### Arquivos afetados
- `apps/web/src/app/api/medicos/importar/route.ts`
- `apps/web/public/templates/medicos-modelo.csv`
- `apps/web/src/server/repositories/medico-repository.ts` (se `criarMedico` precisar aceitar cobrança)
- Testes de importação em `apps/web/tests`

## Testing
- Vitest. Testar `parseCsv`/`rowToInput` com fixtures de CSV (string), sem rede/DB nas partes puras;
  criação mockada quando necessário.

## CodeRabbit Integration
- Story type: **Feature (importação/validação de dados)**.
- Foco: robustez do parsing (linhas malformadas), reuso do schema (sem duplicar regra), preservação
  dos limites de segurança. Severidade: CRITICAL/HIGH auto-fix; edge cases de parsing como MEDIUM.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-01 | 0.1 | Story criada a partir da arquitetura §6 (CSV) | River (@sm) |
| 2026-07-01 | 1.0 | Validada (10/10 pós-fix): +complexidade +riscos. GO. Status Draft→Ready | Pax (@po) |

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List

## QA Results
