# Story 12.19 — Navegação da vertical contábil

## Status
Ready

## Story
**As a** operador da vertical de contabilidade,
**I want** uma entrada "Emissões" dentro da seção Contabilidade da Sidebar,
**so that** eu não precise saber que o relatório de emissão contábil mora dentro da vertical
médica para encontrá-lo.

## Contexto
Fase 3 BAIXA do épico `docs/architecture/epic-sanitizacao-emissoes.md`. Cobre R-23 (G-36),
decisão **D5** do épico (manter `/execucoes` compartilhado + navegação por entrada dedicada, em
vez de tela própria).

**[DEP]** **12.11** (o filtro por tipo de pagador, usado no link pré-filtrado, nasce lá).

## Acceptance Criteria
1. Entrada "Emissões" na seção *Contabilidade* da `Sidebar.tsx` (`:28,:33-39`), apontando para o
   histórico **filtrado por tipo de pagador = cliente contábil**.
2. A divisória de navegação pedida pelo dono em 2026-07-24 (separação Contabilidade/Cobrança
   Médica) passa a valer também para a **saída** da emissão contábil, não só para a entrada.
3. O link usa o filtro "Tipo" criado em 12.11 (`HistoricoExecucoes.tsx`) — sem duplicar lógica de
   filtro.

## Complexidade e Riscos
- **Complexidade:** B — 1 entrada de menu + 1 link com filtro pré-aplicado, sem lógica nova
  (consome o que 12.11 já construiu).
- **Risco:** nenhum além da dependência direta de 12.11 estar fechada (o filtro por tipo precisa
  existir antes de este link fazer sentido).
- **DoD:** AC 1–3 + testes verdes + File List completa.

## Tasks / Subtasks
- [ ] `Sidebar.tsx`: entrada "Emissões" na seção Contabilidade (AC 1, 2)
- [ ] `HistoricoExecucoes.tsx`: link com filtro pré-aplicado (tipo = cliente contábil) (AC 1, 3)
- [ ] Testes: navegação chega no histórico já filtrado corretamente

## Dev Notes
- D5 do épico: opção A decidida (manter `/execucoes` compartilhado + entrada de menu na seção
  Contabilidade), em vez de tela dedicada (opção B, rejeitada por duplicar relatório/histórico/
  filtros/ações em massa sem ganho funcional).
- Consequência explícita do épico: "R-23 (12.19) depende de R-10 (12.11)" — a entrada de menu só
  faz sentido depois que o histórico souber filtrar por tipo de pagador.

### File List (proposto)
- `apps/web/src/components/layout/Sidebar.tsx` (M)
- `apps/web/src/components/execucoes/HistoricoExecucoes.tsx` (M)
- Testes: `Sidebar.test.tsx` (M), `HistoricoExecucoes.test.tsx` (M)

## Testing
Vitest — entrada de menu presente na seção correta, link navega para o histórico com o filtro de
tipo = cliente contábil já aplicado.

## CodeRabbit Integration
- Story type: **Frontend (navegação)**.
- Foco: nenhuma duplicação de lógica de filtro; consistência com a divisória de navegação de
  2026-07-24.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-08-25 | 0.1 | Story criada pelo @sm a partir do Épico 12 (`docs/architecture/epic-sanitizacao-emissoes.md`, Fase 3, story 12.19). Aguarda `*validate-story-draft` do @po antes de @dev iniciar. | @sm |
| 2026-08-25 | 0.2 | @po `*validate-story-draft` (po-master-checklist): GO. Menor story do épico; [DEP] 12.11 correta e justificada por D5 (o filtro por tipo nasce lá); AC 3 impede duplicação de lógica de filtro. Aprovada sem ajuste. Status Draft -> Ready. | @po |
