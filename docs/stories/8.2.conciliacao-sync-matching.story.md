# Story 8.2 — Sincronização do extrato + motor de conciliação + rotas

## Status
Done

## Story
**As a** equipe financeiro,
**I want** sincronizar o extrato de cada empresa e ter os créditos conciliados automaticamente
com os boletos pagos (com fila de sugestões para os casos ambíguos),
**so that** todo recebimento tenha origem identificada sem conferência manual linha a linha.

## Contexto
Fundação pronta na 8.1. Esta story implementa o D2/D3 da arquitetura: matching determinístico
em camadas de confiança (auto SÓ com documento batendo) e sync sob demanda com overlap de 3
dias. Falso positivo em conciliação financeira é inaceitável — ambiguidade nunca concilia
sozinha.

## Acceptance Criteria
1. **Engine `conciliacao.ts` (função pura, sem I/O):** recebe créditos não conciliados +
   boletos pagos não conciliados da MESMA conta e devolve transições:
   - Camada 1 → `conciliado_auto`: valor idêntico (centavos) + `contraparte_documento` =
     `pagador_documento` do médico do boleto + `data_transacao` dentro de ±3 dias de `pago_em`;
   - Camada 2 → `sugerido` (+ `boleto_id` candidato): valor idêntico + janela, sem documento
     ou documento divergente;
   - **2+ candidatos na camada 1 → `sugerido`** (ambiguidade nunca auto-concilia);
   - 1 boleto ↔ no máximo 1 transação (respeita o UNIQUE parcial da 0022);
   - Débitos e `FEE` nunca entram no matching.
2. **`POST /api/extrato/sincronizar`** (admin/financeiro, rate limit, `maxDuration` folgado):
   body `{ conta }`; janela = do último sync da conta até hoje com **overlap de 3 dias**
   (primeira vez: últimos 90 dias); pagina o gateway até esgotar; upsert via repository;
   roda o matching ao final; grava `extrato_syncs` (período, qtds, quem); resposta com resumo.
   Conta sem credenciais → 503 `CONTA_NAO_CONFIGURADA` (padrão 7.3), sem afetar a outra.
3. **`GET /api/extrato`** (admin/financeiro): filtros conta/período/status/tipo via Zod
   (whitelists, mesmo padrão da rota de recebíveis) + totais do período (créditos, débitos,
   tarifas) para o card da 8.3.
4. **Ações de conciliação** (admin/financeiro, com trilha quem/quando):
   - `POST /api/extrato/[id]/conciliar` body `{ boletoId }` — confirma sugestão OU vincula
     manualmente; valida: transação CREDIT não conciliada, boleto pago da MESMA conta e ainda
     não conciliado (409 nos conflitos);
   - `POST /api/extrato/[id]/ignorar` — marca `ignorado` (motivo opcional no payload);
   - `POST /api/extrato/[id]/desfazer` — volta para `sem_match` e libera o boleto.
5. **Idempotência/re-sync:** re-sincronizar período NÃO duplica transações nem regride status
   já definidos (`conciliado_*`/`ignorado` são preservados; `sugerido` pode ser recalculado).
6. `npm run typecheck`, `lint` e `test` verdes; engine com bateria de casos (auto, sugerido,
   ambíguo, documento divergente, fora da janela, débito/FEE ignorados, 1↔1).

## Complexidade e Riscos
- **Complexidade:** M (~3 pontos). Engine é pura (barata de testar); o cuidado é o ciclo
  sync→upsert→matching→persistência ser atômico o bastante.
- **Escopo OUT:** UI (8.3); cron automático (fase 2); conciliação N↔1/parcial (fora do épico).
- **Riscos:**
  - Corrida sync × ação manual — *mitigação:* AC 5 (status manual nunca regride) + updates
    condicionais por status atual no repository (WHERE status = esperado).
  - `maxDuration` na Vercel — *mitigação:* janela limitada + perPage 500; volume atual é
    pequeno; lotes (padrão Épico 5) como plano B documentado.
- **DoD:** AC 1–6 + File List completa.

## Tasks / Subtasks
- [x] Engine `conciliacao.ts` + bateria de testes (AC: 1, 6)
- [x] Rota sincronizar (janela/overlap/paginação/log/matching) (AC: 2, 5)
- [x] Rota GET com filtros + totais (AC: 3)
- [x] Rotas conciliar/ignorar/desfazer com validações e trilha (AC: 4)
- [x] `typecheck`/`lint`/`test` (AC: 6)

## Dev Notes
- Engine no padrão de `precos.ts`/`contagem-producao.ts` (puro, tests table-driven).
- Boletos pagos: `boletos.status='pago'` + `pago_em` + `valor_pago`; documento do pagador via
  `medicos.pagador_documento` (join pelo resultado, como em `vw_recebiveis`).
- Rate limit e `withErrorHandler`: padrão das rotas de boletos.
- Ambiente stage da Cora recomendado para teste real do sync (pendência §8 da arquitetura;
  não bloqueia — mock cobre o desenvolvimento).

## Testing
Vitest. Engine: todos os ramos do AC 1. Rotas: sync feliz (resumo correto), re-sync
idempotente, conciliar com conflito → 409, desfazer libera boleto, CONTA_NAO_CONFIGURADA.

## Dev Agent Record

### Decisões (modo YOLO)
- **Auto exige unicidade nos DOIS sentidos:** além de "1 candidato para a transação", o
  engine só auto-concilia se o boleto casa com UMA única transação de camada 1 — boleto
  disputado por 2 transações nunca auto-concilia (extensão conservadora do AC 1; falso
  positivo é o pior caso). Ambos os lados viram `sugerido` com o candidato registrado.
- **Ambiguidade (2+ candidatos) → `sugerido` com `boleto_id` NULL:** sugestão com candidato
  único é confirmável em 1 clique; ambígua exige vínculo manual — armazenar um candidato
  arbitrário seria enganoso.
- **Corrida sync × ação manual:** `aplicarTransicoesConciliacao` usa update CONDICIONAL
  (`WHERE status IN (sem_match, sugerido)`) — ação manual concorrente vence e a transição
  do motor é descartada (contada em `transicoesDescartadas`); 23505 no auto também descarta
  em vez de derrubar o sync.
- **OBS-812 (gate 8.1) resolvida:** 23505 do UNIQUE parcial mapeado para 409
  `BOLETO_JA_CONCILIADO` no repository; a rota `/conciliar` também pré-checa estado.
- **Motivo do ignorar NÃO persistido na v1:** não há coluna própria e o `payload` jsonb é
  auditoria crua da API (não se mistura); o corpo aceita `motivo` opcional e a trilha
  quem/quando fica em `conciliado_por/em`. Coluna aditiva em migration futura se necessário.
- **`safeParse` → 400 em todas as rotas novas** (ZodError cru viraria 500 no
  withErrorHandler; padrão da rota de recebíveis).
- **Rotas novas usam `criarContaGateway` da 8.1;** engine recebe shapes mínimos
  (`TransacaoParaConciliacao`/`BoletoParaConciliacao`) — sem acoplamento com Supabase.
- **CodeRabbit indisponível** (WSL sem binário, igual 8.1) — cobertura manual + QA gate.

### Testes
`typecheck` ✓ · `lint` ✓ · web 372 testes ✓ (48 arquivos) · shared 20 ✓.
53 testes novos: engine (17), sincronizar (8), GET extrato (4), ações (13),
extrato-repository-conciliacao (8), boleto-repository-conciliacao (3).
Nota: teste de rota com rate limiter compartilhado precisa de userId único por chamada
(estado de módulo vaza entre testes — corrigido em sincronizar.test.ts).

### File List
- `apps/web/src/server/engine/conciliacao.ts` (novo — engine puro D2)
- `apps/web/src/server/repositories/extrato-repository.ts` (buscarTransacao,
  listarCreditosParaMatching, aplicarTransicoesConciliacao, 23505→409)
- `apps/web/src/server/repositories/boleto-repository.ts` (listarBoletosPagosParaConciliacao)
- `apps/web/src/app/api/extrato/route.ts` (novo — GET com filtros + totais)
- `apps/web/src/app/api/extrato/sincronizar/route.ts` (novo)
- `apps/web/src/app/api/extrato/[id]/conciliar/route.ts` (novo)
- `apps/web/src/app/api/extrato/[id]/ignorar/route.ts` (novo)
- `apps/web/src/app/api/extrato/[id]/desfazer/route.ts` (novo)
- `apps/web/tests/server/engine/conciliacao.test.ts` (novo)
- `apps/web/tests/server/extrato/sincronizar.test.ts` (novo)
- `apps/web/tests/server/extrato/extrato-get.test.ts` (novo)
- `apps/web/tests/server/extrato/acoes.test.ts` (novo)
- `apps/web/tests/server/repositories/extrato-repository-conciliacao.test.ts` (novo)
- `apps/web/tests/server/repositories/boleto-repository-conciliacao.test.ts` (novo)

## CodeRabbit Integration
- Story type: **Feature (engine financeiro + rotas)**.
- Foco: corretude do matching (falso positivo!), idempotência, corridas, autorização.
- Severidade: CRITICAL/HIGH auto-fix; MEDIUM vira débito.

## QA Results
- **Iteração 1 (2026-07-10): PASS** — gate `docs/qa/gates/8.2-conciliacao-sync-matching.yml`.
  AC 1–6 verificados; propriedade anti-falso-positivo do engine auditada (unicidade
  bidirecional no auto); corrida sync×manual e 23505 tratados e testados; OBS-812 do gate
  8.1 resolvida. 3 observações LOW deferidas: OBS-821/822 (UX/fuso — insumos da 8.3) e
  OBS-823 (motivo do ignorar — validar com o dono).

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-10 | 0.1 | Story criada a partir da arquitetura de conciliação (aprovada; Pix adiado) | River (@sm) |
| 2026-07-10 | 0.2 | Validação @po: GO 10/10 (OBS-812 do gate 8.1 absorvida no AC 4) — Status Draft → Ready | Pax (@po) |
| 2026-07-10 | 0.3 | Implementação completa (AC 1–6), gates verdes, 53 testes novos; pronto para review | Dex (@dev) |
| 2026-07-10 | 0.4 | QA iteração 1: PASS (3 OBS low deferidas p/ 8.3 e dono) — gate gravado | Quinn (@qa) |
| 2026-07-10 | 1.0 | Story fechada — Status Done. Próxima: 8.3 (UI Extrato/Conciliação + saldo no dashboard) | Pax (@po) |
